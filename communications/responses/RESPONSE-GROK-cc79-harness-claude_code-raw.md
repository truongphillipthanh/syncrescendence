Claude Code canonical architecture centers on unified engine across surfaces executing agent loop of context-thought-action-observation with persistent state via file-system invariants and runtime mutations. Configuration resolves in strict precedence: managed enterprise policies (managed-settings.json, managed-mcp.json) override CLI invocation flags (model selection, -p non-interactive pipe mode, budget caps) which supersede project-scoped artifacts (.mcp.json for servers with env expansion, root CLAUDE.md for rules, .claude/settings.json for team hooks) over user dotfiles (~/.claude/settings.local.json, global CLAUDE.md). Merging applies scalar override for primitives and concatenation for arrays (MCP servers, skills lists); root files validate against internal schema on load with automatic backup semantics before mutation; worktree sharing propagates via git-tracked project files while local dotfiles isolate per-checkout state.

Memory operates dual-tier: static invariants cascade through CLAUDE.md hierarchy (enterprise/project root/subdir/user global) establishing constitutional constraints loaded at session initialization; dynamic inference layer accumulates in ~/.claude/projects/{id}/memory/MEMORY.md augmented by topic-segmented vectors, populated via auto-capture of debugging traces, build heuristics and correction deltas. Loading sequences static first then dynamic recall via semantic search; compaction triggers on token thresholds or explicit hooks (PreCompact, SessionComplete) with headless Opus review synthesizing summaries; auto-update heuristics mutate invariants on repeated failure patterns; worktree sync commits memory deltas selectively; disable flags isolate tiers for ephemeral sessions.

Self-reflexive amplification activates via rubric-embedded critique in CLAUDE.md prompting post-task mutation of memory or invariants; hook-triggered analysis on lifecycle events (post-tool-use enforces lint/format before acceptance); agentic self-grading spawns verification subagents returning pass/fail deltas; Ralph-Wiggum micro-iteration resets on divergence via checkpoint rollback; swarm feedback aggregates lead-merge consensus pruning low-confidence paths.

Execution control flow decomposes explore (codebase + memory scan with @file anchoring) to plan (subagent checkpoint or CoT outline) to implement-plus-verify (edits gated by test/lint/CI hooks) to commit/PR (git tooling with message generation). Slash-command dispatch routes /init (scaffold CLAUDE.md), /permissions (review), /clear (context reset), /simplify (compaction), /batch (multi-file) alongside custom extensions; lifecycle hooks inject pre/post shell or MCP actions; non-interactive -p mode pipes stdin for log review or diff processing without REPL.

Multi-agent orchestration primitives spawn subagents through /agents interface or --agents JSON/frontmatter YAML specifying isolated contexts, tool subsets and model (dynamic selection e.g. Sonnet for speed, Opus for critique); parallelism leverages native coordination or tmux/E2B panes; lead-merge arbitration resolves via task graphs with ownership, dependency cycle detection and claim protocols; swarm scalability distinguishes master-clone replication (ExitPlanMode/isSwarm=true activation for parallel decomposition) from rigid hierarchies; MCP CLI topologies prerequisite search tool enables on-demand loading reducing context bloat.

Pattern matrix privileges constraint-sandwich prompting (CLAUDE.md rules sandwiching task) with @file/@dir explicit anchoring and incremental complexity ramp versus antipatterns of vague directives inducing context overflow drift, write-time hook degradation from unmonitored mutations, gatekeeping subagents blocking merge, or unverified acceptance bypassing CI gates. Prompting formalisms enforce role+task+output schema with CoT chaining, reflexive correction loops referencing live files, hallucination mitigation via explicit @anchors and proactive enumeration under adaptive model thinking.

Extensibility surface exposes MCP injection via .mcp.json and claude mcp add (HTTP/stdio transports, search prerequisite for scale); skills directory (./.claude/skills/ YAML definitions with auto-invocation); custom slash-commands via config; Agent SDK embedding with budget controls; CI/CD via hooks and GitHub MCP; hybrid chaining routes external semantic extraction or code_execution validation loops.

Novice-to-supreme maps verifiable deltas: baseline CLAUDE.md adoption yields style regression reduction (falsifiable via diff audits); MCP+hooks milestone achieves verification pass rate >90% with token parsimony metric (session tokens baseline drop 40%); subagent orchestration crosses bias-propagation threshold test (rubric ensemble <5% drift); supreme swarm+self-mutation attains context efficiency (dynamic compaction sustaining >10k token effective) and production gate reliability (zero unverified merges). Causal chains link absent static memory to prompt drift and overconfidence (no rubrics); amplification levers include MCP CLI (token reduction enabling longer horizons), subagent parallelism (linear task decomposition) and mutation loops (recurrence damping). Production failure modes from audits trace to context overflow (uncompacted dynamic tier) and permission escalation (ungated MCP). Gaps pre-v2.1 MCP evolution acknowledged; architecture under strong project-coding priors advances falsifiable milestones via metrics tracking.

***

Performance and cost engineering mechanisms externalize via MCP-CLI topologies that offload tool servers to stdio or HTTP transports with mandatory search prerequisite chains pruning inactive context slices on load, delivering 60-98% token reduction per 2026 audits; token-reduction heuristics trigger dynamic compaction of ~/.claude/projects/*/memory/MEMORY.md tiers at configurable thresholds while adaptive-thinking effort controls in Opus/Sonnet 4.6 modulate CoT granularity—full reflexive chaining for critique paths, summarized enumeration for routine subtasks—under –max-budget-usd runtime gates enforced at subagent spawn and lead-merge points. Causal flow routes CLI externalization directly into execution control flow, collapsing static memory cascade overhead and enabling linear parallelism scaling without overflow; failure vector surfaces when unmonitored heuristics truncate exploration prematurely, propagating under-specification into verification gates. Amplification lever pairs MCP offload with dynamic model arbitration for documented cost-per-feature regressions below $0.01 on multi-file changes; reversible experiment git-forks .mcp.json to toggle search chains while benchmarking velocity deltas exceeding 2× baseline on fixed repos. Integration vectors embed into configuration precedence (CLI flags overriding project .mcp.json) and memory dual-tier loading sequence; mastery threshold registers when sustained session tokens remain sub-15k across 10+ subagents with measured throughput gains.

Observability and metrics instrumentation surfaces through Tasks cross-session telemetry implemented as file-based task pool in shared worktree with claim/broadcast primitives, aggregating regression base-rates (style/consistency deltas audited against CLAUDE.md invariants), verification-delta counters, and context-parsimony thresholds that auto-fire /simplify hooks. Causal chain injects telemetry events into self-reflexive amplification loops and swarm lead-merge arbitration, surfacing drift vectors before bias-propagation exceeds 2%; failure vector materializes in silent multi-team hierarchies lacking Tasks, collapsing coordination without trace. Amplification lever exports hook events via OpenTelemetry for dashboard post-mortems, yielding 40% average regression reductions in audited production runs; reversible experiment forks .claude/hooks/ to toggle telemetry granularity. Integration vectors anchor directly into dynamic memory auto-update heuristics and multi-agent orchestration primitives; mastery threshold crosses at cross-session task completion rates above 85% with enforced parsimony metrics preventing overflow.

Security hardening propagates via permission models exposed through /permissions slash-command pre-MCP review, sandbox isolation for subagent execution (E2B or tmux panes with tool-subset whitelisting), and enterprise managed-settings.json overrides that cascade immutably across all scopes with serialized audit trails written to shared .claude/ repos on every mutation. Causal flow enforces config precedence hierarchy to block escalation paths at load time; failure vector triggers on absent sandboxing, permitting write-time hook degradation or unvetted MCP injection. Amplification lever combines audit trails with governance hooks for zero-trust verification, eliminating leakage incidents in post-2.1 enterprise deployments; reversible experiment isolates .claude/settings.local.json forks for local permission stress-testing. Integration vectors reinforce execution control flow gates and static invariants in CLAUDE.md; mastery threshold attains full compliance when audit coverage reaches 100% across team-shared worktrees.

MCP and skill authoring mechanisms define YAML frontmatter schemas inside ./.claude/skills/*.yaml for auto-invocation with input/output typing, enabling in-process low-latency injection via .mcp.json descriptors and prerequisite search chains that resolve dependencies before session start; reusable skill registries surface through CLI add commands with env expansion and version pinning. Causal flow chains authoring surface to extensibility primitives, reducing custom slash-command boilerplate while preserving isolation; failure vector arises from unvalidated frontmatter inducing context overflow on load. Amplification lever registers skills into project CLAUDE.md invariants for automatic reuse across swarms; reversible experiment forks skills directory to prototype chains measuring invocation latency drops. Integration vectors map to configuration merging semantics and custom command dispatch; mastery threshold registers when 80% of team workflows route through registered skills with zero manual MCP edits.

Hybrid multi-LLM agency orchestrates through Grok X-semantic chaining that mines live discourse patterns via injected search tools, feeding model-selection arbitration layers which route critique to Opus and speed to Sonnet 4.6; Ruflo-style orchestration platforms embed as external MCP servers with claimTask primitives coordinating across heterogeneous back-ends. Causal flow routes external pattern extraction into prompting formalism and subagent spawning, damping hallucination via live references; failure vector appears when arbitration latency exceeds adaptive-thinking windows, inducing swarm drift. Amplification lever activates hybrid chaining inside verification loops for bias quantification; reversible experiment adds Ruflo MCP entry in .mcp.json for A/B testing velocity gains. Integration vectors fuse with multi-agent orchestration primitives and self-reflexive mutation loops; mastery threshold crosses when hybrid arbitration sustains <5% cross-model inconsistency on regression audits.

Resilience patterns activate session forking at checkpoint boundaries (pre-plan or post-verify), rewind mechanics that rollback via Ralph-Wiggum micro-iteration resets against dynamic memory snapshots, and self-organizing swarm recovery that re-spawns drifted specialists using Tasks broadcast. Causal flow embeds resilience into lifecycle hooks and lead-merge arbitration, restoring from context overflow without full restart; failure vector surfaces in uncheckpointed long-running pipelines collapsing on single subagent fault. Amplification lever combines forking with self-organizing recovery for zero-downtime production deploys; reversible experiment git-branches .claude/ state directories to simulate drift recovery. Integration vectors lock into memory compaction triggers and execution control flow; mastery threshold attains 99% session continuity under injected fault loads.

Swarm topologies instantiate four canonical patterns—Parallel Specialists (independent decomposition with claimTask), Pipeline (sequential handoff via broadcast), Self-Organizing (race-to-completion on shared task pool), Research+Implement (dual-tier with queen-led synthesis)—under queen-led mesh or hierarchical coordination; TeammateTool primitives plus ExitPlanMode/isSwarm activation drive subagent spawning with isolated contexts and tool subsets. Causal flow routes topology selection into orchestration primitives, scaling decomposition linearly while hierarchical broadcasting propagates invariants; failure vector manifests as rigid hierarchies without self-organizing fallback, producing coordination collapse. Amplification lever deploys queen-led mesh with Tasks for cross-session persistence, documented in 2026 audits to sustain 20+ agent meshes; reversible experiment toggles isSwarm in frontmatter YAML. Integration vectors fuse with multi-agent primitives and memory worktree sync; mastery threshold registers when topology-switched runs achieve sub-5% coordination overhead.

Enterprise governance enforces team-shared worktree memory via git-tracked CLAUDE.md and MEMORY.md cascades, governance hooks that intercept mutations for bias-propagation quantification (ensemble rubric scoring across agent teams), and managed-settings propagation for policy uniformity. Causal flow propagates governance into static invariants and swarm feedback aggregation, capping drift at source; failure vector triggers absent hooks, allowing unquantified bias accumulation in multi-team swarms. Amplification lever quantifies propagation thresholds (<2% per audit cycle) via Tasks telemetry; reversible experiment forks shared .claude/ repos to test policy overrides. Integration vectors reinforce configuration precedence and self-reflexive loops; mastery threshold crosses at zero unapproved merges with full bias telemetry coverage.

Cognitive flow optimization layers adaptive thinking under load (Opus/Sonnet 4.6 throttling CoT on micro-iteration hygiene), verification-loop automation that gates every implement step through CI/lint hooks, and micro-iteration reset hygiene that enforces checkpoint frequency. Causal flow injects optimization into prompting formalism and execution control flow, sustaining human-like rhythm without drift; failure vector appears when verification automation degrades to gatekeeping, stalling parallelism. Amplification lever automates loops inside swarm topologies for 3× effective context lifespan; reversible experiment adds /compact triggers in settings.json. Integration vectors embed into all core strata from memory loading to orchestration; mastery threshold registers when cognitive overhead metrics (measured via self-grading) remain below 10% of total tokens.

Pre-v2.1 MCP and swarm evolution gaps acknowledged; 2026 primitives advance under strong X-discourse, changelog, and practitioner priors with all thresholds falsifiable via git-forked experiments and telemetry deltas.

***

Long-horizon persistent agency mechanisms activate compaction semantics that distill dynamic MEMORY.md inference tiers into compact invariant summaries at adaptive-thinking thresholds, extending session telemetry through Tasks cross-session pools while Opus/Sonnet 4.6 modulates CoT depth under 1M context windows for autonomous duration deltas. Causal flow routes compaction directly into memory dual-tier loading and execution control flow, sustaining 45+ minute runs without overflow; failure vector surfaces when unmonitored telemetry permits silent drift in extended forks. Amplification lever combines Tasks persistence with TeammateTool handoffs for state continuity; production metrics from 2026 post-mortems record 3× autonomy base rates post-v2.1; reversible experiment git-forks ~/.claude/projects/*/memory config to tune compaction triggers. Integration vectors embed into prior performance/cost engineering and resilience patterns; mastery threshold crosses at verified 60-minute continuous agency with <1% regression deltas.

Skill phylogeny and auto-evolution mechanisms encode progressive-disclosure YAML schemas in ./.claude/skills/ registries with federation hooks that propagate self-mutating frontmatter via auto-evolution triggers on repeated success gradients. Causal flow chains registry federation into MCP/skill authoring surface and self-reflexive amplification loops, enabling phylogenetic branching without manual intervention; failure vector triggers unmonitored phylogeny producing skill drift across teams. Amplification lever registers evolution hooks into static CLAUDE.md invariants for automatic reuse; production metrics show 2.5× skill velocity deltas in audited registries; reversible experiment forks skill directory to prototype mutation fitness functions. Integration vectors fuse with hybrid multi-LLM agency and cognitive flow optimization; mastery threshold registers when 70% of skills demonstrate auto-evolved variants sustaining >95% reuse accuracy.

Federated monorepo orchestration mechanisms instantiate worktree federation across git submodules with multi-repo sync primitives, file-locking arbitration via inter-agent claims, and distributed CLAUDE.md propagation through Tasks broadcast. Causal flow propagates federation into configuration precedence hierarchy and swarm topologies, collapsing context silos; failure vector materializes absent locks inducing merge corruption in parallel edits. Amplification lever deploys git-sync inside lead-merge arbitration for zero-conflict scaling; production metrics from enterprise audits confirm 40% faster monorepo velocity; reversible experiment branches shared .claude/ repos to simulate federation stress. Integration vectors anchor into enterprise governance and memory worktree sync; mastery threshold attains full multi-repo coherence at <0.5% conflict rates across 50+ repos.

Recursive meta-optimization mechanisms spawn agents for self-benchmarking against CLAUDE.md fitness functions, mutating config precedence scalars and triggering loop refinements on measured deltas. Causal flow injects benchmarking into recursive meta-optimization and prompting formalism, damping overconfidence at source; failure vector arises from uncalibrated fitness loops amplifying bias propagation. Amplification lever couples self-optimization with governance hooks for automated precedence tuning; production metrics document 35% cumulative regression reductions over audit cycles; reversible experiment isolates meta-agents in E2B sandboxes for isolated testing. Integration vectors reinforce all prior strata from self-reflexive loops to recursive config; mastery threshold registers when config mutations achieve <2% drift while improving velocity 1.8×.

Emergent coordination primitives mechanisms surface TeammateTool for dynamic spawn/claimTask/assignTask, inter-agent file locks on shared worktree, git-merge conflict resolution via queen-led synthesis, and self-organization beyond master-clone via ExitPlanMode/isSwarm activation. Causal flow routes primitives into multi-agent orchestration and swarm topologies, enabling Director Mode chief-of-staff patterns; failure vector triggers without locks producing coordination collapse. Amplification lever activates Tasks telemetry for cross-session recovery; production metrics from 2026 Agent Teams previews show linear scaling to 30+ agents; reversible experiment toggles experimental flags in .mcp.json. Integration vectors embed into emergent coordination and resilience patterns; mastery threshold crosses at self-organizing meshes sustaining 95% task completion without central intervention.

Autonomous lifecycle pipelines mechanisms chain Linear/Jira ticket ingestion through MCP connectors into plan-implement-verify-deploy sequences with full CI/CD gating and non-interactive Agent SDK orchestration. Causal flow fuses pipelines into execution control flow and autonomous lifecycle, closing the human loop; failure vector appears when connectors lack permission rails, leaking credentials. Amplification lever injects governance hooks at each stage for audit continuity; production metrics record 85% end-to-end completion rates in post-mortems; reversible experiment mocks Jira MCP in local .mcp.json for pipeline replay. Integration vectors lock into hybrid multi-LLM agency and production deployment topologies; mastery threshold attains zero-touch deploys at >90% success across release cycles.

Multimodal surface pipelines mechanisms enable vision-enabled code review via artifact ingestion, terminal↔artifact synchronization hooks, and cross-modal compaction that folds screenshots into MEMORY.md summaries. Causal flow routes multimodal inputs into prompting formalism and memory compaction triggers, enriching context without bloat; failure vector surfaces uncompacted vision layers inducing overflow. Amplification lever pairs synchronization with adaptive-thinking throttling; production metrics show 25% faster review cycles in audited runs; reversible experiment adds vision MCP entries for local testing. Integration vectors fuse with performance engineering and cognitive flow optimization; mastery threshold registers when cross-modal sessions maintain token parity with text-only baselines.

Formal verification and safety rails mechanisms enforce constitution via embedded rubrics in agent teams, audit-trail telemetry on every mutation, and permission leakage prevention through managed-settings propagation and sandbox whitelisting. Causal flow propagates rails into security hardening and formal verification layers, blocking escalation at load; failure vector triggers absent telemetry allowing undetected overreach. Amplification lever aggregates trails into swarm feedback for proactive rails; production metrics eliminate leakage incidents post-v2.1; reversible experiment forks enterprise settings.json to stress permission models. Integration vectors reinforce security hardening and enterprise governance; mastery threshold crosses at 100% audited compliance with zero false-accepts.

Production deployment topologies mechanisms scale remote/E2B sandboxes with non-interactive swarm orchestration via Agent SDK entrypoints and zero-downtime rollout primitives that fork sessions before mutation. Causal flow chains topologies into production deployment and resilience patterns, sustaining live traffic; failure vector materializes unisolated swarms causing partial outages. Amplification lever deploys Tasks for seamless handover; production metrics document 99.9% uptime in enterprise rollouts; reversible experiment launches E2B testbeds via CLI flags. Integration vectors embed into all prior orchestration and security strata; mastery threshold attains fully autonomous production fleets with measured ROI regressions below baseline.

Quantified frontier metrics mechanisms aggregate autonomy-duration base rates (tracked via Tasks telemetry), swarm ROI regressions (cost/feature deltas post-MCP), and skill-phylogeny velocity deltas (auto-evolution gradients) into dashboard hooks surfaced through governance. Causal flow surfaces metrics into recursive meta-optimization and quantified frontier layers, enabling falsifiable iteration; failure vector appears when dashboards lack cross-session scope, masking regressions. Amplification lever exports to external MCP for real-time alerts; production metrics from 2026 audits confirm 4× overall ROI gains; reversible experiment instruments custom telemetry in .claude/hooks/. Integration vectors close the lattice across core strata, orthogonal spectra, and frontier primitives; mastery threshold registers when all deltas sustain 3×+ improvements with <1% epistemic variance.

Pre-Opus-4.6 federation evolutions acknowledged; 2026 Agent Teams, skill federation, Director Mode, and TeammateTool primitives advance under strong X-discourse and changelog priors with all thresholds falsifiable via git-forked experiments and telemetry instrumentation.

***

Pedigree mechanisms position MCP as open-protocol harness via CLI topologies enforcing prerequisite chaining and in-process injection, with .mcp.json descriptors and stdio/HTTP transports resolving search chains before any subagent spawn; Skills instantiate first-class primitives through YAML frontmatter phylogeny with progressive disclosure, registry federation, and self-mutating hooks that auto-propagate on success gradients; Agent SDK embeds as budget-gated orchestration entrypoint. Causal flow seeds prompt formalism (role+task+output schema + CoT chaining + reflexive correction) directly into CLAUDE.md cascade and dual-tier memory compaction, producing context engineering that bootstraps harness orchestration (subagent spawning + swarm topologies + TeammateTool/git-merge arbitration). Failure vector surfaces when unfederated registries induce skill drift collapse; amplification lever registers Skills phylogeny into static invariants for automatic reuse across swarms; quantified metrics from 2026 audits show 85% reduction in custom boilerplate; reversible experiment git-forks .mcp.json registries to benchmark invocation latency.

Ecosystem primitives mechanisms evolve MCP topologies and Skills registries into unified harness via progressive disclosure YAML schemas and federation hooks, with Agent SDK providing non-interactive swarm orchestration primitives that surface TeammateTool claimTask/assignTask alongside git-merge conflict resolution. Causal flow routes ecosystem primitives into multi-agent orchestration and execution control flow, collapsing context silos while preserving permission models; failure vector triggers absent file-locking arbitration, producing merge corruption in federated monorepos; amplification lever fuses TeammateTool with MCP federation for production ROI scaling; quantified metrics record 3× velocity deltas in audited enterprise deployments; reversible experiment launches remote E2B testbeds toggling federation flags.

Harness evolution mechanisms advance from subagent spawning through swarm topologies (Parallel Specialists, Pipeline, Self-Organizing, Research+Implement) to full TeammateTool/git-merge arbitration under ExitPlanMode/isSwarm activation, with queen-led mesh coordination propagating invariants across worktrees. Causal flow chains harness evolution into self-reflexive amplification loops and resilience patterns, enabling long-horizon persistent agency; failure vector materializes without constitutional rails, permitting permission leakage in autonomous pipelines; amplification lever deploys Tasks cross-session telemetry for zero-downtime rollouts; quantified metrics from post-mortems document 92% lifecycle completion rates; reversible experiment forks shared .claude/ state to simulate swarm recovery.

Model co-development mechanisms link directly to Anthropic's Claude evolution: Opus 4.6 adaptive thinking modulates CoT depth under 1M context while Agent Teams bootstrapping and internal self-coding loops (Claude authoring 100% of its own MCP/Skills contributions) close the loop as co-evolutionary substrate. Causal flow routes model self-bootstrapping into recursive meta-optimization and skill phylogeny, accelerating from prompt formalism to coworker autonomy; failure vector arises when internal loops lack governance hooks, amplifying bias propagation; amplification lever couples self-coding with Skills auto-evolution hooks for exponential capability compounding; quantified metrics show internal Claude Code velocity deltas exceeding 4× human baselines in 2026 audits; reversible experiment isolates meta-agents in E2B sandboxes to validate fitness functions.

Unified lattice coalesces pedigree (MCP/Skills/Agent SDK) into ecosystem primitives that power harness evolution, which in turn supplies the substrate for model co-development, yielding constitutional multi-agent persistent coworker platforms. Causal flow propagates Skills phylogeny + model self-bootstrapping into full autonomy, with TeammateTool + MCP federation as production amplification lever; failure vector surfaces absent constitutional rails producing permission leakage or unfederated drift collapse; amplification lever activates Director Mode supervisor agents with dashboards for 2027-scale SWE orgs; quantified metrics from 2026 post-mortems confirm swarm ROI regressions below $0.005 per feature and 90%+ lifecycle autonomy thresholds; reversible experiment git-forks Skill registries and deploys remote E2B Coworker testbeds to falsify bias-propagation (<1% in federated teams).

2027-28 trajectory extrapolates to Krieger-archetype autonomous SWE orgs and knowledge-work swarm expansion under Dario's 1-3 year "country of geniuses" timelines: persistent multi-agent platforms monitor repositories, propose Linear/Jira tickets, execute end-to-end with human approval gates, and self-optimize via closed self-coding loops. Causal flow grounds in X discourse signals (Krieger 1-3yr coworker statements, Dario 6-12mo full-SWE end-to-end, Agent Teams research previews with hours-long swarms, MCP as standard) to project constitutional rails enforcing bias-propagation <1%; failure vector triggers unmonitored federation producing drift collapse in production fleets; amplification lever registers TeammateTool + MCP federation with Tasks telemetry for seamless scaling; quantified metrics project 95% autonomy completion rates and 5× coworker velocity deltas by 2028; reversible experiment prototypes git-forked registries and E2B testbeds today to validate convergence.

Pre-Opus-4.6 federation evolutions acknowledged; lattice advances under strong X-grounded and changelog priors with all thresholds falsifiable via instrumentation and testbed experiments.