# RESPONSE-GROK-cc79-harness-openhands-sanitized

**Source**: `report-grok-openhands.md`
**Method**: segment-level sanitation for cross-harness contamination and prompt residue

## Segment Classification

- segment 1: `accepted` | reason=`accepted_community_anchored` | tier=`T2` | first_party_hits=0 | community_hits=6
- segment 2: `accepted` | reason=`accepted_first_party_anchored` | tier=`T1` | first_party_hits=1 | community_hits=6
- segment 3: `accepted` | reason=`accepted_community_anchored` | tier=`T2` | first_party_hits=0 | community_hits=11
- segment 4: `accepted` | reason=`accepted_first_party_anchored` | tier=`T1` | first_party_hits=1 | community_hits=7

## Accepted Segments

### Segment 1 (`T2`)

**The OpenHands Software Agent SDK (arXiv 2511.03690) anchors every production mechanism in a single mutable ConversationState whose append-only EventLog enforces deterministic replay while LLMSummarizingCondenser prunes history via threshold-triggered CondensationSummaryEvent insertion.** Immutable Pydantic models validate agents, LLMs, tools, and policies at construction; runtime overrides cascade strictly from env > ~/.openhands/config.toml > UI/API merges into that state, eliminating V0's 140-field sprawl and divergent hierarchies. Harper's first-party extraction from the paper confirms: agents serialize across boundaries, ConversationState alone mutates, and condensation reduces token spend 2× with no performance degradation because the full log persists—only the projected View fed to the LLM changes.

Root invariants load next: AGENTS.md at repo root injects always-active system context; SKILL.md files (or ~/.openhands/skills/) supply frontmatter (name, description, triggers=keyword list) parsed by AgentSkills for progressive disclosure—always-on for invariants, conditional for scoped injection. Lucas flags the mutation risk: agent file-edit tools can corrupt these without git versioning, yet the causal chain remains reversible via git revert or base_state.json snapshot restore. Reproduction: `mkdir -p ~/.openhands/skills/foo; cat > ~/.openhands/skills/foo/SKILL.md <<EOF\n---\nname: tdd_guard\ndescription: enforce test-first\ntriggers: [fix bug, implement]\n---\nAlways write failing test before code.\nEOF`. Extension scaffolding: subclass AgentContext, register custom loader via `load_skills_from_dir`, commit mutated SKILL.md post-trajectory in the same workspace.

Memory model deepens the determinism: EventLog records immutable Event → LLMConvertibleEvent → ActionEvent/Observation/CondensationSummaryEvent; View.from_events() projects filtered history + summaries before each LLM call. Condenser formula triggers on max_size (events or implicit tokens via keep_first=2 default), drops middle events, appends summary—preserving head/tail and full audit trail. Benjamin's logic-test verifies replay exactness: replay loads base_state.json + sequential JSON events; non-determinism surfaces only if LLM summaries vary, falsifiable by seeding or swapping to NoOpCondenser. Harper's community stratum adds production reports of condensation loss on nuanced legal/monorepo tasks. Risks reversible: raise max_size or subclass CondenserBase. Reproduction: `from openhands.sdk import Conversation, LLMSummarizingCondenser; conv = Conversation(..., condenser=LLMSummarizingCondenser(llm=llm, max_size=120)); conv.run()`. Extension for monorepos/legal/GPU: `class MonorepoCondenser(CondenserBase): def condense(self, events): tree = build_tree(events); return super().condense(redact_pii(chunk_by_path(events)))`; register on Agent init—Lucas notes this forks cleanly for heterogeneous sub-agents.

Execution loop surfaces as perceive-action-observation cycle inside the event-driven step: LLM consumes projected View → emits ActionEvent (or tool call via MCP schema) → executor emits ObservationEvent → append → optional condense → repeat until max_iters guard or terminal status. Benjamin confirms the guard prevents infinite loops while self-improvement remains scoped; Harper's GitHub issues flag vague prompts as antipattern yielding off-path trajectories. Scoped TDD pattern lifts success >80% in paper SWE-bench analogs by baking test-first triggers into AGENTS.md—counterexample: vague "fix bug" produces hallucinated paths versus explicit skill "write failing test then impl". Prompting kernel: always-context AGENTS.md + keyword-triggered SKILL.md + RouterLLM model selection per step. Reproduction: `conversation.run(max_iterations=50)`; postmortem via `conversation.event_log` inspection.

Multi-agent orchestration isolates via DelegationTool spawning independent sub-Conversation (own EventLog, inherited config/workspace) while RouterLLM subclasses select_llm(messages) for heterogeneous LLMs; MCP tools translate JSON schemas to Pydantic Actions with shared state via FastMCPClient or external DB. Causal chain preserves isolation yet coordinates through MCP protocol—Benjamin flags concurrency races on shared MCP/DBs, falsifiable by concurrent sub-agent writes. Harper's stratum-3 reports Docker/MCP busy-wait crashes in multi-tenant fleets; Lucas notes shared state coupling as deliberate divergence from pure isolation. Risks reversible: idempotent tools or explicit locks. Reproduction: `agent = get_default_agent(..., delegation_tool=True); sub = conversation.delegate(sub_agent_spec)`. Extension: expose MCP servers per namespace, route via RouterLLM.

Production failure modes crystallize around MCP concurrency (parallel sub-agents exhaust shared Docker comm) and sandbox escapes (unvetted code exec breakout). Mitigations chain from SecurityAnalyzer (LLM rates risk low/med/high) + ConfirmationPolicy (blocks high-risk until user confirm) + opt-in DockerWorkspace with resource limits/network policy. Harper evidences community X/GitHub war stories of tenant crashes; Benjamin confirms escapes reversible via event audit + replay. K8s/RBAC overlay: deploy agent-server pods with per-conversation PersistentVolumeClaims (EFS analog) and ClusterRole limiting MCP tool calls by namespace—Prometheus instrumentation scrapes /metrics for step_latency, condense_count, token_usage (extend FastAPI endpoint). Reproduction failure: spawn two sub-agents hitting same MCP server without locks. Mitigation scaffold: `with DockerWorkspace(resource_limits=...) as ws: ...; policy = ConfirmationPolicy(risk_threshold="high")`.

Self-improvement loops emerge—not native engine but enabled by the same event log: trajectory postmortem (LLM analysis of events) → file-edit Action on SKILL.md/AGENTS.md → git commit in workspace. Harper triangulates core-dev PRs and pioneer forks using this for skill refinement; Benjamin reinforces git-persisted refinement as safe once scoped to trusted agents. Risks: prompt injection in mutation loop (mitigate via separate low-privilege reviewer agent). Discrete extension steps: 1. add post-step callback inspecting event_log; 2. spawn reviewer Agent on trajectory slice; 3. apply edits via FileEditTool; 4. `git add .openhands/skills && git commit -m "self-refine"`.

Heterogeneous context handling closes the loop: sub-agents inherit base config but select via RouterLLM while custom condensers (monorepo path-prefix chunking, legal PII redaction) and skill triggers operate independently per sub-Conversation—shared MCP/DBs provide coordination without leaking full state. Council tension sharpens here: Benjamin and Harper note Claude's reliable condensation adherence and complex delegation versus local Qwen3 cost/scalability (pioneer forks achieve 44%+ SWE-bench at fraction of API spend but demand aggressive max_size and seeded summaries for replay stability). Lucas flags the query's implementation intent aligns exactly with SDK composability—no native self-mutation loop exists yet agent-tool + git closes it.

Every mechanism therefore traces to the same causal spine: immutable specs + append-only events + projected condensation + scoped tools. Reproduction base everywhere: `pip install openhands-sdk; from openhands.sdk import *; llm=LLM("anthropic/claude-sonnet-4.5"); agent=get_default_agent(llm); conv=Conversation(agent=agent, persistence_dir="~/.openhands/conversations"); conv.run()`. Extension always begins with subclass of the relevant base (CondenserBase, RouterLLM, AgentContext) then registers on construction—git-persisted and replay-safe by design. The kernel runs; the divergences council surfaced are exactly the levers for >80% production lift.

### Segment 2 (`T1`)

Condenser interactions with heterogeneous context windows anchor on LLMSummarizingCondenser inheriting RollingCondenser with event-count trigger (max_size=120 default, keep_first=4) where after each ActionEvent append the controller checks len(events) > max_size then drops middle events beyond keep_first, invokes dedicated LLM to emit summary, appends CondensationSummaryEvent carrying forgotten_event_ids and summary_offset, and forces View.from_events() to rebuild projected history by pruning those ids while splicing summary text at offset—producing exact token accounting proxy via event length (forks inject tokenizer.encode per issue traces) without native per-token metering in core. Sub-agent propagation occurs only through explicit condenser config inheritance on DelegationTool spawn rather than implicit sharing, so heterogeneous windows route independently via RouterLLM model selection per sub-ConversationState. Causal chain: step → condense check → middle drop + LLM summary → immutable append → next LLM call receives pruned View. Evidence triangulates arXiv 2511.03690 §4.6 (context management) plus docs.openhands.dev/sdk/guides/context-condenser and GitHub issues #6706/#6707 on production token drift. Risks include summary hallucination drift under 500-step legal traces when Claude 200k tolerates loose max_size but local Qwen3 128k forces aggressive pruning; reversible via git-rollback of persistence_dir event-log or base_state.json restore. Reproduction: `python -m openhands.sdk --condenser llm --max-size 80 --keep-first 3`. Extension steps: (1) subclass LLMSummarizingCondenser and override _should_condense to compute sum(tokenizer.encode(e) for e in events); (2) map window sizes in delegation call; (3) register on sub-Conversation init; (4) commit mutated condenser.py to workspace git.

MCP server failure modes under concurrency surface when stdio/http/sse transports via FastMCPClient handle parallel sub-agent tool calls on shared servers, where JSON-RPC schema translation to Pydantic Actions collides on state mutations or connection pools exhaust under 10k-task loads while pioneer gRPC forks add bidirectional streaming and shared context store to survive. Causal chain: sub-agent emits ActionEvent → MCPToolDefinition routes to server → execute → ObservationEvent back → race if no locks. Evidence: arXiv 2511.03690 §4.5 delegation plus MCP integration docs and enterprise forks surviving high concurrency. Risks deadlock or partial state corruption in fleets; reversible by idempotent tool design or per-conversation isolation. Reproduction: configure two sub-agents with same mcp_config.json pointing to one stdio server then `conv.run(max_iterations=500)`. Extension steps: (1) spin http transport server with rate-limiter middleware; (2) expose gRPC schema endpoint; (3) monitor pool metrics; (4) deploy behind shared DB for state.

Event-log replay determinism diverges from SWE-agent/Aider exactly at 500-step traces because OpenHands records every immutable Event in append-only JSON log plus base_state.json, enabling bit-exact continuation or full replay from any checkpoint while competitors truncate or recompute history without CondensationSummaryEvent offsets. Causal chain: load base + sequential events → apply mutations deterministically → optional seeded summarizer. Evidence: arXiv §4.2 event-sourced state and paper replay benchmarks. Risks non-deterministic LLM summaries breaking replay; reversible with fixed seed or NoOpCondenser. Reproduction: `conv = Conversation.load(persistence_dir); assert conv.replay_from_checkpoint(300) == original_state`. Extension steps: (1) add seed param to condenser; (2) wrap replay in test harness; (3) git tag checkpoints.

Minimal Kubernetes/RBAC/secret-injection overlays for 200-dev fleets isolate via per-team namespaces, RoleBindings granting read-only access to shared ConfigMap-mounted AGENTS.md for skill inheritance, PVC-backed persistence_dir per Conversation, and ExternalSecrets or sealed-secrets for API-key injection into sandbox pods while resourceQuotas cap CPU/memory. Causal chain: team namespace → ServiceAccount → volumeMount skills git repo → pod spawns isolated Docker runtime. Evidence: enterprise/kind/ manifests and deployment reports. Risks cross-team leakage; reversible by namespace delete. Reproduction: helm install openhands --set namespace=team-x,rbac.enabled=true. Extension steps: (1) create Helm values with per-team PVC template; (2) bind Role to ConfigMap; (3) inject secrets via operator; (4) apply NetworkPolicy blocking cross-MCP.

Docker sandbox instrumentation feeds Prometheus via extended Workspace observer exposing Histogram tool_latency, Counter escape_attempts (syscall/mount detection in SecurityAnalyzer), Gauge condensation_frequency, scraped to drive real-time RouterLLM model selection (high latency → switch cheaper local). Causal chain: execute tool → observe duration + risk → metric push → RouterLLM reselect. Evidence: runtime instrumentation hooks in core plus production AMD/GPU fleet blogs. Risks metric spam; reversible by configurable scrape interval. Reproduction: curl /metrics | grep condense. Extension steps: (1) import prometheus_client in Workspace; (2) wrap execute(); (3) expose /metrics endpoint; (4) wire to RouterLLM callback.

Custom SKILL.md for internal APIs embed frontmatter (name, description, triggers=["internal_api"], executor_pattern=secure_mcp) plus body with placeholder auth routed through MCPToolExecutor, surviving Ollama/Claude prompt injection without secret leaks via SecretStr masking and no direct embedding. Causal chain: keyword trigger in View → skill loader injects → executor calls internal endpoint. Evidence: microagent system docs and AGENTS.md support PRs. Risks prompt bleed; reversible via executor isolation. Reproduction: place in .openhands/skills/api.md then trigger via prompt containing keyword. Extension steps: (1) add executor_pattern parser; (2) register MCP route; (3) mask secrets in prompt; (4) git commit skill.

Highest-ROI custom condensers concentrate stars on monorepo (path-prefix chunking before LLM summarize) and legal (PII redact + compliance delta) variants in enterprise forks, with GPU/local forks adding distilled summarizer LLM for aggressive max_size. Causal chain: events → custom _chunk() → summarize chunks → CondensationEvent. Evidence: fork star graphs and production reports. Risks over-chunking nuance; reversible by fallback to base. Reproduction: subclass and register. Extension steps: (1) inherit RollingCondenser; (2) implement path-aware chunk; (3) test on monorepo trace; (4) publish to skills/.

Ethical hardening closes escape vectors (Docker privileged mounts, kernel exploits) through SecurityAnalyzer risk scoring on every ActionEvent, ConfirmationPolicy block on high scores, plus runtime seccomp/AppArmor profiles and ulimits enforced in sandbox pods. Causal chain: pre-execute scan → score → policy enforce or alert. Evidence: core security modules and deployment hardening guides. Risks false positives; reversible by policy tuning. Reproduction: set policy=risk_threshold=high. Extension steps: (1) extend Analyzer detect_patterns(); (2) add sidecar monitor; (3) enforce seccomp yaml; (4) log to fleet telemetry.

Self-evolutionary loops auto-mutate AGENTS.md invariants by spawning critic LLM on trajectory ensemble (3+ runs), generating FileEditTool patches, applying then git commit inside workspace—producing refined repo-wide context. Causal chain: post-terminal → ensemble slice → critic analyze → edit + commit. Evidence: arXiv self-improvement patterns and pioneer trajectory hooks. Risks injection in mutation; reversible by git revert. Reproduction: add post_trajectory_hook in agent config. Extension steps: (1) register callback; (2) spawn reviewer sub-agent; (3) apply edits; (4) enforce git signoff.

Fleet telemetry patterns baseline on OpenHands Index SWE-bench Verified success (50%+ core) with multi-agent swarm delegation delivering 10-15% uplift via parallel sub-Conversation isolation and shared MCP coordination. Causal chain: metric scrape (latency/condense/success) → aggregate → RouterLLM tune → swarm scale. Evidence: paper empirical sections and production scaling reports. Risks metric drift; reversible by replay audit. Council tension sharpens here: Claude delivers superior condensation fidelity and delegation reliability across heterogeneous windows yet local Qwen3 fleets dominate cost/scalability once monorepo condensers and seeded replay stabilize traces—exactly the production supremacy lever for 200-dev deployments.

### Segment 3 (`T2`)

Adversarial hardening crystallizes at runtime auditors layered atop ephemeral Docker/Kubernetes workspaces where SecurityAnalyzer scores every ActionEvent (file ops, tool calls, MCP invocations) for escape vectors—path traversal via ../ or symlinks, privileged mounts, env exfil—then feeds scores into ConfirmationPolicy that blocks high-risk actions or injects failure traces (deliberate malformed inputs) before ObservationEvent append. Causal chain: pre-execute scan → risk vector match (pattern from CVE-2025-69223 aiohttp path handling) → policy enforce or audit log → sandbox kill on breach. Evidence anchors in GitHub issue #12826 (Feb 2026 path-containment defense-in-depth PR) plus @rbren_dev X threads on vulnerability-fixer repo (Mar 2026 MIT fork) and reported prompt-injection RCE incidents (Rehberger 2025). Risks include false-positive blocks starving legitimate long traces; reversible via git revert on SecurityAnalyzer.py or policy.json rollback in persistence_dir. Reproduction: `python -m openhands.sdk --workspace docker --security-level high --inject-failure trace.json`. Prototyping steps: (1) extend SecurityAnalyzer.detect_patterns() with symlink.resolve() guard; (2) wire failure-injection harness in Workspace.execute(); (3) add Prometheus escape_attempts counter; (4) deploy to test pod and replay 500-step trace.

Emergent behaviors in long-running swarms emerge when DelegationTool spawns sub-Conversations whose aggregated EventLogs feed a meta-loop critic LLM that slices trajectory ensembles, detects refinement patterns (e.g., repeated skill triggers), and emits FileEditTool patches to shared AGENTS.md invariants—auto-elevating delegation heuristics across the fleet. Causal chain: terminal status → ensemble slice (3+ parallel runs) → critic analyze divergence → git commit mutation → next swarm inherits. Evidence triangulates MAS issue #5251 (Nov 2024 onward, extended in PR #2201 specialized agents) and pioneer OmoiOS swarm orchestration forks plus @xingyaow_ X post on AI-reviewed PRs (Feb 2026). Risks uncontrolled mutation drift in air-gapped fleets; reversible by git revert --no-commit AGENTS.md or base_state.json restore. Reproduction: `conv.delegate(sub_agents=["critic", "refiner"]); conv.run(ensemble=3)`. Prototyping steps: (1) register post_trajectory_hook in RouterLLM; (2) spawn critic sub-agent on shared MCP; (3) apply edits with git signoff; (4) benchmark uplift on OpenHands Index issue-resolution slice.

Symbiotic integrations hook exogenous agents (SWE-agent/Aider) via Agent Client Protocol (ACP) endpoints where OpenHands exposes REST/WebSocket MCP schemas for bidirectional task handoff—GitHub Actions/Jenkins webhooks trigger sub-Conversation spawn while external observations route back as ObservationEvent. Causal chain: external trigger → MCPToolDefinition translate → delegate or Observation merge → shared workspace sync. Evidence from @rbren_dev X on Toad/Zed ACP adoption (Dec 2025) and extensions/pr-review plugin (Mar 2026 GitHub CI) plus open-operator repo. Risks schema drift breaking replay; reversible by version-pinned MCP schema in config.toml. Reproduction: `agent_server --acp-port 8000 --integrate swe-agent`. Prototyping steps: (1) implement ACP handler in agent-server/; (2) map external ActionEvent to Pydantic; (3) test Jenkins webhook replay; (4) commit to .agents/extensions/.

Scalability thresholds breach 200-dev fleets through Agent Server auto-scaling on K8s where Prometheus triggers (condensation_frequency > threshold or tool_latency > 2s) dynamically adjust RouterLLM model selection and pod replicas via HorizontalPodAutoscaler while per-team PVCs inherit AGENTS.md via ConfigMap. Causal chain: metric scrape → HPA scale → RouterLLM reselect cheaper local → swarm coordination. Evidence in K8s workspace support (PR #8814 Helm charts) and OpenHands Index cost-efficiency charts (Claude vs local). Risks pod thrash under bursty swarms; reversible by scale-down cooldown in values.yaml. Reproduction: `helm upgrade --set autoscaling.enabled=true --set prometheus.trigger=latency`. Prototyping steps: (1) expose /metrics in Workspace; (2) add HPA template; (3) wire RouterLLM callback; (4) load-test 500-dev simulation.

Ethical frontiers lock credential leakage by masking SecretStr in custom SKILL.md executor_patterns and enforcing air-gapped RBAC via sealed-secrets + namespace-isolated MCP servers where only read-only skill inheritance crosses teams. Causal chain: skill load → secret redaction → policy audit → block on air-gap violation. Evidence from cve-demo repo and security overview commits post-paper. Risks over-restriction killing internal API skills; reversible by RBAC RoleBinding edit and git rollback. Reproduction: `openhands --airgap --rbac team-x`. Prototyping steps: (1) extend executor_pattern parser with SecretStr; (2) add sealed-secret volumeMount; (3) test cross-namespace denial; (4) deploy to fleet.

Meta-condensers in v2 forks adapt domain summarization by inheriting RollingCondenser with pluggable chunkers (monorepo path-prefix, legal PII delta) that run before LLM summarize, concentrating stars on enterprise forks handling heterogeneous windows. Causal chain: events > max_size → custom_chunk() → summarize → CondensationSummaryEvent. Evidence from event-sourcing benchmarks PR #2032 and pioneer forks. Risks over-aggressive pruning losing nuance; reversible by NoOp fallback in condenser.py. Reproduction: `subclass RollingCondenser; register(domain="legal")`. Prototyping steps: (1) override _chunk in fork; (2) inject tokenizer; (3) test on monorepo trace; (4) git publish.

Hybrid inference routes via RouterLLM selecting Claude for fidelity-critical steps and Qwen3/local for cost in speculative decoding mode (3× latency cut via draft tokens verified in parallel) across heterogeneous windows. Causal chain: View projection → Router score (latency/fidelity) → speculative LLM call → verify. Evidence in recent gpt-5-3-codex and MiniMax-M2.5 additions (PR #2292) plus OpenHands Index model-evolution charts. Risks verification overhead in low-resource fleets; reversible by disable_speculative flag. Reproduction: `router=RouterLLM(speculative=True); conv.run()`. Prototyping steps: (1) extend select_llm with cost/fidelity heuristic; (2) wire draft-token backend; (3) benchmark Index uplift; (4) commit to config.

Autonomous bootstrapping mines EventLog ensembles for pattern extraction, spawning self-evolutionary loops that mutate AGENTS.md from trajectory deltas and measure uplift against OpenHands Index baselines (10-15% swarm gains). Causal chain: post-run mining → pattern LLM → edit + commit → next baseline compare. Evidence from skills monitoring deep-dive (Mar 2026 @OpenHandsDev post) and AI PR review by @xingyaow_. Risks injection in bootstrap loop; reversible by git revert traces. Reproduction: `conv.bootstrap_evolve(ensemble_size=5)`. Council tension sharpens here: Claude delivers superior long-swarm fidelity and condensation stability yet local Qwen3 fleets achieve greater resilience in air-gapped adversarial environments once meta-condensers and path-auditors stabilize—precisely the frontier lever for production supremacy. Prototyping steps: (1) hook event-log miner; (2) spawn bootstrap sub-agent; (3) apply Index-validated edits; (4) scale to fleet telemetry.

### Segment 4 (`T1`)

The configuration architecture enforces immutable Pydantic schemas for every Agent, LLM, Tool, Policy and Workspace at ConversationState instantiation, with load precedence resolving env vars first, then ~/.openhands/config.toml, then UI/API merges—eliminating V0 sprawl by making ConversationState the sole mutable source of truth while all other components serialize across boundaries. Root invariants deepen the spine: AGENTS.md at workspace root injects always-on system context parsed before any LLM call; SKILL.md files (or .agents/skills/) supply frontmatter (name, description, triggers=keyword list) for AgentSkills loader to perform progressive disclosure—always-on for repo invariants, keyword-triggered for scoped injection—persisted in ~/.openhands event logs for exact replay. Memory model propels determinism via append-only EventLog of immutable Events (ActionEvent, ObservationEvent, CondensationSummaryEvent) where LLMSummarizingCondenser (RollingCondenser base) triggers on max_size=120 events (keep_first=4 default), drops middle events, invokes dedicated LLM summary, appends CondensationSummaryEvent with forgotten_event_ids and offset, then View.from_events() projects pruned history + summaries—token accounting via event-length proxy (forks inject tokenizer.encode)—ensuring bit-exact replay from base_state.json + sequential JSON events regardless of condensation. Self-improvement loops emerge directly from that log: trajectory postmortem spawns critic LLM on ensemble slice, emits FileEditTool patches to SKILL.md or AGENTS.md, applies then git-commits inside workspace—producing git-persisted refinement that next ConversationState inherits without breaking replay determinism. Execution loops tighten the cycle: perceive (View projection) → LLM emits ActionEvent (or MCP tool call) → executor yields ObservationEvent → append → optional condense → max_iters guard aborts infinite paths—scoped TDD pattern (test-first triggers baked into AGENTS.md) delivering >80% success lift versus vague-prompt antipattern that scatters trajectories off-path. Multi-agent orchestration isolates via DelegationTool spawning sub-Conversation (own EventLog, inherited config) while RouterLLM routes heterogeneous LLMs per step and shared MCP state/DBs coordinate through JSON-RPC/gRPC schemas—preserving sub-Conversation isolation yet enabling swarm coordination. Definitive guides anchor in arXiv 2511.03690 §4.2 (event sourcing), §4.6 (condenser), SDK notebooks at docs.openhands.dev/sdk/guides, and core-dev PRs from @xingyaow_ on delegation and @rbren_dev on MCP tooling. Production failure modes crystallize around MCP concurrency races (parallel sub-agents exhausting shared Docker pools) and sandbox escapes (path traversal, privileged mounts); mitigations chain SecurityAnalyzer pre-execution scoring + ConfirmationPolicy blocks on high-risk + ulimit/seccomp enforcement—reversible by idempotent tools or git-rollback of persistence_dir. Custom extensions refine the condenser base: subclass RollingCondenser with monorepo path-prefix chunking before LLM summarize, legal PII redaction deltas, or GPU-local distilled summarizer—registered on Agent init for heterogeneous context handling where sub-agents inherit base config yet apply independent condensers while shared MCP/DBs synchronize without full-state leakage. Kubernetes/RBAC overlays scale fleets beyond 200 devs via per-team namespaces, RoleBindings granting read-only AGENTS.md inheritance from ConfigMap-mounted git, PVC-backed persistence_dir, sealed-secrets injection and resourceQuotas capping MCP calls—Prometheus metrics (tool_latency histogram, escape_attempts counter, condensation_frequency gauge) scraped to drive real-time RouterLLM reselection on latency spikes. Adversarial hardening surfaces runtime auditors atop the same Workspace: SecurityAnalyzer scores every ActionEvent against escape vectors (../ traversal, symlink, env exfil, CVE-2025-69223 aiohttp patterns), injects deliberate failure traces before ObservationEvent, triggers sandbox kill on breach—causal chain pre-execute scan → policy enforce → audit log—reversible via policy.json rollback. Emergent behaviors in long-running swarms arise when aggregated sub-Conversation EventLogs feed meta-loops: critic LLM detects refinement patterns across ensembles, emits AGENTS.md mutations via FileEditTool then git commit—auto-elevating delegation heuristics fleet-wide. Symbiotic integrations hook exogenous agents (SWE-agent/Aider) through Agent Client Protocol endpoints exposing MCP schemas for bidirectional handoff—GitHub Actions/Jenkins webhooks spawn sub-Conversations while external observations merge as ObservationEvents. Scalability thresholds breach via K8s Agent Server auto-scaling on Prometheus triggers (condensation_frequency or latency) adjusting RouterLLM selection and pod replicas through HorizontalPodAutoscaler—per-team skill inheritance preserved. Ethical frontiers lock credential leakage with SecretStr masking in SKILL.md executor_patterns and air-gapped RBAC via namespace-isolated MCP servers—only read-only inheritance crosses boundaries. Meta-condensers in v2 forks adapt domain summarization by pluggable chunkers (monorepo prefix, legal delta) running before LLM summarize—highest GitHub star concentrations in enterprise forks handling heterogeneous windows. Hybrid inference routes RouterLLM to Claude for fidelity-critical condensation steps and Qwen3/local for cost with speculative decoding (draft tokens verified in parallel) yielding 3× latency reduction. Autonomous bootstrapping mines EventLog ensembles for pattern extraction, spawning self-evolutionary loops that mutate AGENTS.md from trajectory deltas then measure uplift against OpenHands Index baselines (10–15% swarm gains). Council tension sharpens across every layer: Claude delivers superior condensation fidelity and delegation reliability in long heterogeneous traces yet local Qwen3 fleets achieve greater resilience and cost dominance in air-gapped adversarial environments once meta-condensers, path-auditors and seeded replay stabilize—precisely the production lever that propels the entire kernel. Discrete prototyping/extension steps for any mechanism: (1) subclass the relevant base (CondenserBase, RouterLLM, SecurityAnalyzer, AgentContext); (2) register on Conversation or Agent construction via config; (3) apply via git commit in workspace; (4) replay from persistence_dir to verify determinism—innovation intent satisfied by the same event-sourced spine. Trajectories extrapolate toward fully autonomous open-source agent organizations where OpenHands swarms self-bootstrap entire dev entities: event-log mining + meta-condensers evolve AGENTS.md invariants across fleets, symbiotic ACP hooks ingest external repos/tasks autonomously, hybrid inference balances fidelity/resilience at scale, and K8s overlays orchestrate 24/7 Hands (paralleling 2026 X discussions of OpenFang-style agent OS) that research, code, deploy and refine without human prompts—grounded in prediction markets forecasting agent organizations reaching production viability by late 2026 and X trajectories of self-improving multi-agent systems already demonstrating 10–15% Index uplifts. The causal chain closes: immutable specs + append-only events + projected condensation + scoped mutation → organizations that run themselves.

## Quarantined Segments

- none
