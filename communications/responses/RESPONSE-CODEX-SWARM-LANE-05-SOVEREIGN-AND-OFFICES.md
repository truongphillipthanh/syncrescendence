# Response

**Response ID**: `RSP-20260306-sovereign-and-offices`
**Surface**: `codex_parallel_session`
**Date received**: `2026-03-06`
**Dispatch packet**: `communications/prompts/PACKET-CODEX-SWARM-LANE-05-SOVEREIGN-AND-OFFICES.md`
**Result state**: `complete`
**Receipt artifacts**:
  - `communications/responses/RESPONSE-CODEX-SWARM-LANE-05-SOVEREIGN-AND-OFFICES.md`

## Returned Content

The old shell's sovereign and agent surfaces should be recovered by burden class, not by reviving their root topology. The main conclusion is:

- old `-SOVEREIGN` split across two real functions: live executive steering and async escalation/decision intake
- old `agents/*` already embodied the lawful predecessor of the current `offices/` lane
- old root `-INBOX` and `-OUTBOX` were not a separate institution; they were a leaky transport layer that duplicated office-local and communications work
- the surviving meaning is institutional and documentary, but the condemned part is the physical logistics pattern that let authority hide in private queues and mixed folders

## 1. Old Sovereign Artifacts: Executive Surface vs Pedigree

| Old artifact family | Best successor | Why |
|---|---|---|
| `SOVEREIGN-TRAJECTORY.md`, durable state-of-the-union or strategic narrative files | `executive/briefings/` | These are live steering syntheses, not ancestry-only specimens. |
| `SOVEREIGN-016-ESCALATION-RESOLUTIONS.md`, `ESCALATION-SUMMARY-*`, unresolved decision briefs, blocking incidents needing sovereign judgment | `executive/escalations/` | They express active conflict, blockage, ratification, and cross-lane resolution. |
| subsystem-wide management or summit-style reviews, including older summit bundles mentioned in the archaeology | `executive/summits/` | Their burden is cabinet review across ministries, not office-local execution. |
| historical `SOVEREIGN-NNN-*` decisions whose operative content is over but whose logic shaped the redesign | `pedigree/` | Their current value is constitutional ancestry and decision lineage. |
| `README.md` from old `-SOVEREIGN` | split: live escalation doctrine into `executive/`; topology history into `pedigree/` | It mixes lawful function with now-rejected root logistics. |
| archived sovereign files, commander reinit notes, thread extraction decisions, old platform deployment rulings | mostly `pedigree/`; promote only still-binding live rulings into executive lanes | Most are predecessor evidence unless they still govern present action. |
| low-signal alert stubs such as `ALERT-adjudicator-202602210746.md` | not executive by default; office/platform or runtime evidence only if still useful | A terse alert is not sovereign doctrine merely because it landed in `-SOVEREIGN`. |

Rule of thumb:
- if the artifact still bears current steering burden, it belongs in `executive/`
- if it mainly explains how the shell became what it is, it belongs in `pedigree/`

## 2. Agent-Local Patterns That Should Deepen `offices/`

The strongest predecessor pattern is the office shape itself:

- `inbox/{pending,active,blocked,done,failed}` should remain the canonical local work lifecycle
- `memory/` should stay local, but only as cache, journal, and sync state rather than hidden canon
- `scratchpad/` remains valid for unfinished working matter
- `platform/` should continue to hold harness-local contracts, templates, and logs
- `outbox/` should stay split by artifact class, especially `dispatches`, `receipts`, and `results`

The old offices also proved that local artifact classes were real operational forms, not naming noise:

- `TASK` should remain the local dispatch envelope
- `RECEIPT` should remain the claim or acceptance signal
- `RESULT` should remain the substantive completion artifact
- `CONFIRM` should remain the closure/acknowledgment artifact between offices
- `EXECLOG` should remain office-local execution evidence unless promoted into federal communications or runtime
- `BRIEFING` and `ALERT` are useful local forms, but they should promote upward only when they cross into executive or federal lineage value

The `agents/*/INIT.md` files also contained one recoverable pattern: each office had an explicit burden, jurisdiction, escalation boundary, and completion contract. That meaning should deepen office doctrine, but those instructions should not regain authority as hidden office-only constitutions.

## 3. Communications Lineage vs Local Office Memory

### Should become communications lineage

- prompt-like and packet-like office artifacts with enduring lineage value
- returned `RESULT` artifacts when they answer a prompt, dispatch, or packet beyond one office
- `CONFIRM` artifacts when they close a routed exchange across offices
- handoff-quality state transfers
- executive-facing briefings or escalations
- assessment-style artifacts such as corpus insight reports when they evaluate a run or condition for the wider shell

### Should remain local office memory

- unfinished task envelopes that have not crossed an office boundary
- office-local `EXECLOG` tails and harness logs
- scratch investigations, provisional notes, and temporary coordination
- local journal/cache material in `memory/`
- low-signal alerts or machine-specific diagnostics that do not change federal understanding

### Queue surface disposition

- old root `-INBOX` should not be revived; its real surviving meaning is split between `offices/*/inbox/` and `communications/` lineage
- old root `-OUTBOX` should not be revived; its surviving meaning is split between `offices/*/outbox/` and `communications/responses`, `handoffs`, or `logs`
- `collab/` is pedigree for a shared-project pattern, not a sovereign or office queue successor

## 4. Predecessor Habits Too Topology-Bound To Preserve

- root-dashed queues such as `-SOVEREIGN`, `-INBOX`, and `-OUTBOX`
- letting one folder serve simultaneously as decision queue, incident bin, archive, and executive narrative surface
- treating office-local memory or outbox files as the only copy of shell reasoning
- letting a terse alert inherit sovereign authority just because of where it was dropped
- duplicating task state across root queues and office queues
- encoding authority through path mystique rather than artifact class, lineage, and promotion rules

## Hidden-Authority Risks

1. Old `-SOVEREIGN` normalized private authority accumulation: decisions, incidents, and narratives cohabited one place, so filing location could silently imply legitimacy.
2. Old office outboxes could become shadow communications lanes if receipts, results, and confirms were never promoted.
3. Root `-INBOX` duplicated office intake and created parallel truth about what was active, failed, or done.
4. `INIT.md`-style office doctrine can become covert law if office-local instructions drift away from federal law.
5. Incident summaries and rate-limit escalations can quietly become strategic steering if they stay only in one office or one sovereign folder.

## 5. Ranked Migration List For Sovereign/Office Recovery

1. Treat all recoverable `agents/*` structure as the primary pedigree for `offices/`, especially inbox lifecycle, local memory boundaries, and outbox classing.
2. Promote still-live sovereign decision briefs and escalation bundles into `executive/escalations/`, leaving concluded precedent in `pedigree/`.
3. Promote durable strategic narrative and state-of-the-union matter into `executive/briefings/`; keep superseded worldview documents in `pedigree/`.
4. Rehouse summit-style subsystem reviews into `executive/summits/` or `pedigree/` depending on whether they still steer present ministries.
5. Normalize `TASK`, `RECEIPT`, `RESULT`, `CONFIRM`, `ALERT`, `BRIEFING`, and `EXECLOG` as office artifact classes with explicit promotion thresholds.
6. Audit old root `-INBOX` and `-OUTBOX` contents by lineage and reclassify them into either office-local residue or federal communications artifacts; do not preserve the roots themselves.
7. Treat old sovereign readme/doctrine as split material: extract lawful executive semantics, preserve the root-queue history only as pedigree evidence.
8. Demote topology-bound debris such as path-driven alert dumps, duplicate queue copies, and mixed archive/active folders; preserve only evidentiary specimens when they explain failure modes.

## Net Recommendation

The successor shell should recover the old sovereign shell as two things, not one: `executive/` for live steering burden and `pedigree/` for constitutional ancestry. It should recover the old `agents/*` shell much more directly as `offices/`, because that predecessor pattern was structurally sound once detached from repo-root queue duplication.

The thing to preserve is not the aura of `-SOVEREIGN`; it is the lawful separation of executive judgment, communications lineage, and office-local burden. The thing to reject is any topology where authority can hide in a special folder rather than declare itself through class, lineage, and promotion.

## Immediate Notes

- The highest-value recovery target is office doctrine and artifact classing, because that meaning is already physically compatible with the current shell.
- The highest-risk recovery target is sovereign matter, because it is easiest to mis-preserve as hidden authority or nostalgic root logistics.

## Open Ambiguities

- Some old sovereign incident files may belong in `runtime/` or office `platform/logs/` rather than `executive/` or `pedigree/`; the deciding question is whether they changed institutional judgment or merely recorded machine state.
- Some office `RESULT` and `CONFIRM` specimens may fit better as `communications/responses` versus `communications/logs`; the deciding question is whether they answer a dispatch or merely document chronology.
