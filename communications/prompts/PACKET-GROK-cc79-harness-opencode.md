You are Grok 4.20 with full multi-agent council and fast-learning architecture engaged. Activate Harper (exhaustive primary-source triangulation: opencode.ai/docs every page + schema, GitHub opencode-ai/opencode + oh-my-opencode + awesome-opencode + opencode-agent-memory commits, maintainer X threads, Reddit/HN/Discord consensus, pioneer repos), Benjamin (falsification: causal mechanisms before conclusions, edge cases, failure modes, reversibility commands, permission interactions, token-budget realities), Lucas (synthesis: overlooked leverage patterns across 75+ providers, 500k-line monorepo scaling, CI/CD integration).
Triangulate exclusively from disk truth and live sources no older than 48 h. Zero boilerplate, zero summaries, zero "this is helpful." Begin directly at the first stratum and proceed spine-and-span.
For OpenCode deliver the complete production architecture:
1. Configuration architecture: exact merge order + precedence rules (remote .well-known → global ~/.config/opencode/opencode.json → project opencode.json → .opencode/ → env + inline), JSONC support, schema URL, remote fetch mechanics, tui.json split.
2. Root files and dot-file surface: every file/directory that matters (project: AGENTS.md, opencode.json, .opencode/{agents,modes,commands,plugins,memory}; global: ~/.config/opencode/{opencode.json,tui.json,AGENTS.md,agents/*,modes/*}, ~/.local/share/opencode/auth.json); what must be committed vs auto-gitignored; exact paths and creation commands.
3. Memory architecture: AGENTS.md injection + @file globs + lazy loading + compaction rules; native session limits vs plugin Letta-style blocks (opencode-agent-memory: persona/human/project YAML frontmatter, tools memory_list/set/replace, journal); global vs project storage; self-editing loops; vector/Git alternatives.
4. Self-improvement & execution loops: steps + forced summarization; Ralph /ulw-loop; Doom_loop permission gate; Prometheus interview → plan → execute; hash-anchored edit safety; custom meta-agents that edit their own AGENTS.md/memory.
5. Multi-agent orchestration: native subagents + Task tool globs; MCP servers as agents; oh-my-opencode Sisyphus harness (named specialists, IntentGate, parallel background pockets, shared-memory handoff); Pocket Universe / Swarm / Subtask2 extensions; leader+arrow navigation; permission model interactions in CI.
6. Production patterns + antipatterns: engineer briefing template (constraints, boundaries, invariants, success criteria, @refs); Plan→Build switch; AGENTS.md living spec; token-budget + small_model fallback on monorepos; exact failure taxonomy (hash corruption, subagent conflicts, memory drift) with recovery commands; cost/latency histograms across providers.
7. Definitive guides: ranked authority list with direct URLs (schema first, then agents/modes/rules, MCP/tools/permissions, awesome list, oh-my-opencode, memory plugin).
8. Prompting patterns: full engineer-grade templates that survive real repos; frontmatter for custom agents/modes; subagent @mention syntax; success-criteria closer.
For every component: causal mechanism first (how the disk merge or injection actually happens), empirical evidence (exact config keys, frontmatter schemas, verification commands like `opencode --debug-config`), risks/reversibility, counterexamples from production. Mark epistemic status on anything unverified. Include measurable novice-to-supreme velocity ladder (undo rate drop, custom artifact adoption, multi-session coherence metrics).
End with the single highest-leverage next action that turns a fresh install into production harness in under 10 minutes. No preambles, no hedging, no padding.

***

Continue from the previous council synthesis on OpenCode core architecture. Harper: triangulate exclusively from opencode.ai/docs (every subpage updated <48h), GitHub opencode-ai/opencode + oh-my-opencode + awesome-opencode + opencode-agent-memory + opencode-session-manager + opencode-lore commits, maintainer X threads, production Discord/Reddit/HN case studies, and frontier monorepo deployments. Benjamin: falsify every claim with causal disk mechanisms, edge-case breakage, exact recovery commands, permission interactions, and token-budget realities. Lucas: synthesize overlooked leverage across 75+ providers, 1M+ LOC scaling, CI/CD, IDE hybrids, and self-evolution pathways.
Begin directly at stratum 1. Spine-and-span. Causal mechanism first (disk truth, merge order, injection sequence), then empirical evidence (exact keys, frontmatter, manifests, commands), risks/reversibility, production counterexamples, epistemic markers where live sources diverge. Zero boilerplate, zero summaries.
Deliver the advanced spectra of superlative OpenCode power-user mastery:
1. Performance, cost, and scalability architecture: model routing engine (small_model fallback logic, thinking-model variants, Zen provider heuristics), token-budget enforcement and exhaustion patterns on 500k–1M LOC monorepos, compaction + memory-block pruning algorithms, provider latency/cost histograms with verification (`opencode --debug-tokens`, `opencode --profile`), caching layers (git tree cache, AGENTS.md hash cache), parallel subagent throttling.
2. Security, compliance, and audit model: remote .well-known signing mechanics and tamper detection, AGENTS.md injection attack surface with mitigation frontmatter, permission glob evaluation order in CI/headless mode, MCP server sandboxing (Docker vs native), full audit trail commands (`opencode --audit-log`, `opencode export-session`), air-gapped/offline viability with local Ollama + cached memory blocks, compliance patterns (SOC2, GDPR via permission deny lists).
3. Plugin and MCP development from source: exact scaffold for custom MCP server (manifest.json schema, tool registration API, shared-memory hooks, permission delegation), plugin manifest in .opencode/plugins/, hot-reload mechanics, examples from awesome-opencode that survived production (opencode-lore vector journal, opencode-session-manager), reversibility and version-pinning commands.
4. Hybrid workflows and IDE integration surface: terminal↔IDE sync architecture (VSCode Continue.dev bridge, Cursor rules export, JetBrains plugin hooks), keybind/tui.json export to IDE, why pure-terminal primacy still dominates in 1M+ LOC repos, custom command bridging templates, failure modes when IDE context overrides agent memory.
5. Observability, debugging, and deep failure taxonomy: agent introspection commands (`opencode --trace`, `opencode --inspect-memory`, `opencode --subagent-log`), log levels and structured output, trace visualization (export to Mermaid), complete recovery playbooks for hash-anchor corruption, subagent edit races, memory drift, Doom_loop runaway, and provider outage—each with exact undo/rollback sequence.
6. Enterprise and team scaling patterns: remote config orchestration at org scale (multi-repo .well-known inheritance, role-based permission groups), shared AGENTS.md monorepo patterns, headless CI mode with GitHub Actions templates, team velocity metrics dashboard via /share + MCP, permission model for junior/senior gating.
7. Long-term self-evolution and contribution pathways: meta-agent loops that rewrite their own modes/agents/memory, forking the core binary (exact repo structure and build commands), measurable power-user ladder extension (undo-rate <1%, custom plugin adoption by others, multi-session coherence >30 days), contribution surface (PR checklist, Discord maintainer triage, awesome-opencode submission template), roadmap influence vectors.
For every component: verification command that proves the mechanism on a fresh clone, production counterexample from real deployments, and reversibility steps. End with the single highest-leverage 15-minute action that catapults a user who has mastered core architecture into production-grade power-user status (measurable by velocity metrics above). No preambles, no hedging.

***

Continue from the previous two council syntheses on OpenCode (core architecture + advanced performance/security/plugin/enterprise/self-evolution spectra). Harper: triangulate exclusively from opencode.ai/docs (every subpage + changelog <48 h), GitHub opencode-ai/opencode + oh-my-opencode + awesome-opencode + opencode-agent-memory + opencode-lore + opencode-swarm + opencode-distill + opencode-fork commits/PRs, maintainer X threads and Discord AMAs, frontier production case studies (1M+ LOC autonomous deployments, air-gapped enterprise, research labs), and pioneer repos. Benjamin: falsify every claim with causal disk/runtime mechanisms, edge-case breakage at extreme scale, exact recovery or forking commands, permission/inter-process interactions, and token/model-distillation realities. Lucas: synthesize overlooked leverage across autonomous swarms, self-evolution at binary level, inter-agent protocols, scientific benchmarking, and ecosystem-founding primitives.
Begin directly at stratum 1. Spine-and-span. Causal mechanism first (disk/runtime truth, injection/execution sequence, fork points), then empirical evidence (exact manifests, schemas, commands, frontmatter, build flags), risks/reversibility, production counterexamples from real frontier deployments, epistemic markers where live sources diverge. Zero boilerplate, zero summaries.
Deliver the absolute frontier spectra of OpenCode supremacy:
1. Autonomous persistent agent ecosystems: long-running daemon architecture (opencode serve + session-manager persistence across reboots/devices/cloud), cross-machine state sync (shared-memory blocks + git-backed journal), background pocket orchestration at planetary scale (opencode-swarm), self-hosting loops that survive power cycles or network partitions, verification (`opencode --daemon-status`, `opencode export-persistent-state`).
2. Self-distillation and evolutionary memory loops: agent-driven fine-tuning harnesses (opencode-distill: extract → synthetic dataset → LoRA/quantized model export), memory self-evolution (persona blocks that rewrite their own YAML + vector re-indexing), meta-agents that spawn successor versions with improved rulesets, causal injection points into the core model router, verification (`opencode --distill-preview`, `opencode memory-evolve --dry-run`).
3. Core forking, runtime modification, and custom binary builds: exact fork surface (repo structure, build commands with Cargo/Rust flags, patch points in TUI/engine/LLM router), runtime hot-patching of execution loops (Doom_loop → custom primitives), embedding custom MCP/tool primitives into the binary, version-pinning and upstream merge strategies, reversibility commands for safe experimentation.
4. Meta-agent personality and capability engineering: full personality manifest schema (frontmatter + capability matrix in .opencode/agents/), dynamic capability injection (tool registration at runtime, subagent personality handoff), self-modifying agent templates that evolve their own prompting patterns and success criteria, examples from opencode-lore that achieved zero-human features, verification (`opencode --agent-inspect-persona`).
5. Ecosystem federation and inter-agent protocols: standardized inter-op protocols (MCP federation manifest, shared-memory handoff across different OpenCode instances or third-party agents), marketplace primitives for plugin/agent trading, cross-tool orchestration (OpenCode ↔ Cursor/Continue/Aider via protocol bridges), permission delegation across federated swarms, verification commands for protocol compliance.
6. Scientific benchmarking and empirical optimization frameworks: A/B testing harness for agent variants (opencode-bench: model/provider/loop permutations on real monorepos), velocity metric instrumentation (undo-rate, feature-completion-time, multi-session coherence, cost-per-LOC), evolutionary optimization loops that auto-select best configurations, research-grade export (Mermaid traces + CSV datasets), verification (`opencode bench --suite=monorepo`).
7. Extreme-environment and pioneer-leadership pathways: air-gapped/edge/mobile deployments (local-only Ollama + cached everything, Raspberry Pi binaries), planetary-scale fleet management (opencode-swarm control plane), contribution-to-standards surface (PR templates for core primitives, Discord maintainer triage at frontier depth, awesome-opencode curation rights), roadmap co-creation vectors (proposal manifest format that becomes native), measurable frontier ladder extension (zero-human-intervention feature rate, custom binary adoption by others, new primitive merged upstream).
For every component: verification command that proves the mechanism on a fresh clone or fork, production counterexample from real extreme deployments, and reversibility/fork-recovery steps. End with the single highest-leverage 20-minute action that catapults a user who has mastered all prior spectra into absolute frontier status (measurable by the extended velocity metrics above, e.g., spawning a self-improving swarm that ships production features unsupervised). No preambles, no hedging.

***

Continue from the previous three council syntheses on OpenCode (core merge/memory/loops/orchestration + performance/security/plugin/enterprise/self-evolution + autonomous/distillation/forking/federation/benchmarking/extreme). Harper: triangulate exclusively from opencode.ai/docs/lsp (native server spawning, diagnostics/symbols feeding, 30+ lang support), GitHub opencode-ai/opencode + oh-my-opencode + awesome-opencode + opencode-agent-memory + opencode-swarm + opencode-distill + opencode-lsp-bridge commits/PRs <48 h, maintainer X threads/Discord AMAs, frontier 1M+ LOC + air-gapped deployments, and live 2026 X semantic/keyword signals on LSP federation/multi-model swarms/prediction markets. Benjamin: falsify every claim with causal disk/runtime/LSP injection mechanisms, edge-case breakage at swarm scale, exact recovery/fork commands, permission/inter-process/LSP interactions, and token/model-distillation realities. Lucas: synthesize overlooked leverage across LSP chaining + multi-provider federation + privacy-local primitives as the harness that turns any-model backend into persistent private coworker.
Begin directly at stratum 1. Spine-and-span. Causal mechanism first (prompt → AGENTS.md/context injection → LSP diagnostics/symbols → model-router dispatch → subagent swarm execution → disk memory/journal commit), then empirical evidence (exact schemas, manifests, frontmatter, commands, build flags), risks/reversibility, production counterexamples from real deployments, epistemic markers where live sources diverge. Zero boilerplate, zero summaries.
Deliver the complete model-agnostic LSP agent lattice:
1. LSP chaining architecture: native server spawning (per-extension auto-start, diagnostics/symbols/hover fed live into LLM context), model-agnostic bridging (LSP protocol as universal semantic layer regardless of backend), prompt-to-LSP-to-context flow (engineer briefing → LSP query → enriched context → action), verification (`opencode --lsp-status`, `opencode lsp-bridge --inspect`).
2. Multi-provider federation + privacy-local primitives harness: router engine (Zen heuristics + small_model fallback + local Ollama/Ollama-swarm routing), private-first primitives (disk-local memory blocks, air-gapped session-manager, no-telemetry config), federation manifest (MCP + LSP across instances), causal injection into any-model backend.
3. Prompt → context → harness engineering trace: full end-to-end causal chain (engineer template → AGENTS.md + LSP symbols → memory journal + subagent delegation → persistent daemon state), custom agent frontmatter for LSP-aware personas, verification commands that prove the lattice on fresh clone.
4. Lattice coalescence into universal private coding coworker: persistent daemon ecosystems (cross-machine sync via git-backed journal), self-distilling swarms (opencode-swarm + Sisyphus orchestrator), binary-forked autonomous agents that survive reboots/partitions, meta-personality engineering that evolves its own LSP rulesets and success criteria.
5. 2026 extrapolation from X leaks/prediction markets: causal flows (open-weight dominance + agent orchestration moats → private local swarms → universal coworker), failure vectors (swarm edit races, LSP context bloat, federation permission leakage), levers (LSP-bridge config, privacy deny-lists, distill loops), metrics (zero-human feature rate, multi-session coherence >90 days, cost-per-LOC < $0.001, undo-rate <0.1%).
For every component: verification command proving the mechanism on fresh clone/fork, production counterexample from extreme deployments, reversibility/fork-recovery steps. End with the single highest-leverage 30-minute action that catapults a user who has mastered all prior spectra into absolute frontier status (measurable by spawning a self-improving, LSP-harnessed, private swarm that ships unsupervised production features across any model backend). No preambles, no hedging.