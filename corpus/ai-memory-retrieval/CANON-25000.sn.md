---
id: [[CANON-25000]]
name: Memory Architecture
tier: lattice
celestial_type: planetary
volatility_band: moderate
sn_version: 2.0
parent: CANON-20000
requires: ['CANON-00000', 'CANON-00005']
synthesizes: ['CANON-25100', 'CANON-25200']
entities: ['Memory Architecture (STR)', 'Memory Sovereignty Principle (CON)', 'Seven Memory Strata (STR)', 'Constitutional Memory (CON)', 'Preference Memory (CON)', 'Accumulated Memory (CON)', 'Project/Space Memory (CON)', 'Canonical Knowledge RAG (CON)', 'Thread Context (CON)', 'Tool-Extended Memory (CON)', 'CLI-Foyer Architecture (STR)', 'CLAUDE.md Hierarchy (STR)', 'Distillation Protocol (PROTO)', 'Thread Culmination Protocol (PROTO)', 'Corpus Update Protocol (PROTO)', 'Forgetting Protocol (PROTO)', 'IIC Memory Configuration (PROTO)']
original_words: 7550
original_lines: 1182
---

# Κ-25000: Memory Architecture (SN)

PASS Canon25000MemoryArchitecture:
    sutra: "← compress CANON-25000: MEMORY ARCHITECTURE"
    gloss:
        ← semantic compression required
end

PROC UniversalProtocolForCognitive:
    sutra: "← compress Universal Protocol for Cognitive Persistence"
    gloss:
        ← semantic compression required
end

PASS LatticeTierNavigationInfrastructure:
    sutra: "← compress Lattice Tier | Navigation Infrastructure"
    gloss:
        ← semantic compression required
end

PASS PartIConstitutionalFoundation:
    sutra: "← compress PART I: CONSTITUTIONAL FOUNDATION"
    gloss:
        ← semantic compression required
end

PASS APurposeStatement:
    sutra: "← compress A. Purpose Statement"
    gloss:
        ← semantic compression required
end

NORM BTheMemorySovereignty:
    sutra: "← compress B. The Memory Sovereignty Principle"
    gloss:
        ← semantic compression required
end

PASS CRelationshipToThree:
    sutra: "← compress C. Relationship to Three Horizons"
    gloss:
        ← semantic compression required
end

PASS PartIiPlatformagnosticMemory:
    sutra: "← compress PART II: PLATFORM-AGNOSTIC MEMORY TAXONOMY"
    gloss:
        ← semantic compression required
end

PASS ATheSevenMemory:
    sutra: "← compress A. The Seven Memory Strata"
    gloss:
        ← semantic compression required
end

PASS BMemoryInteractionPatterns:
    sutra: "← compress B. Memory Interaction Patterns"
    gloss:
        ← semantic compression required
end

PASS PartIiiTheClifoyer:
    sutra: "← compress PART III: THE CLI-FOYER ARCHITECTURE"
    gloss:
        ← semantic compression required
end

PASS ARationale:
    sutra: "← compress A. Rationale"
    gloss:
        ← semantic compression required
end

PASS BTheFoyerPattern:
    sutra: "← compress B. The Foyer Pattern"
    gloss:
        ← semantic compression required
end

PASS CClaudemdHierarchy:
    sutra: "← compress C. CLAUDE.md Hierarchy"
    gloss:
        ← semantic compression required
end

PASS DCrossplatformMemoryCoordination:
    sutra: "← compress D. Cross-Platform Memory Coordination"
    gloss:
        ← semantic compression required
end

PROC PartIvTheDistillation:
    sutra: "← compress PART IV: THE DISTILLATION PROTOCOL"
    gloss:
        ← semantic compression required
end

PASS ATheProblemOf:
    sutra: "← compress A. The Problem of Accumulation"
    gloss:
        ← semantic compression required
end

PASS BTheDistillationCascade:
    sutra: "← compress B. The Distillation Cascade"
    gloss:
        ← semantic compression required
end

PROC CThreadCulminationProtocol:
    sutra: "← compress C. Thread Culmination Protocol"
    gloss:
        ← semantic compression required
end

PROC DCorpusUpdateProtocol:
    sutra: "← compress D. Corpus Update Protocol"
    gloss:
        ← semantic compression required
end

PROC EForgettingProtocol:
    sutra: "← compress E. Forgetting Protocol"
    gloss:
        ← semantic compression required
end

PASS PartVIicMemory:
    sutra: "← compress PART V: IIC MEMORY INTEGRATION"
    gloss:
        ← semantic compression required
end

PASS APeriicMemoryConfiguration:
    sutra: "← compress A. Per-IIC Memory Configuration"
    gloss:
        ← semantic compression required
end

PASS BCrossiicSynchronization:
    sutra: "← compress B. Cross-IIC Synchronization"
    gloss:
        ← semantic compression required
end

PASS PartViPlatformspecificImplementation:
    sutra: "← compress PART VI: PLATFORM-SPECIFIC IMPLEMENTATION"
    gloss:
        ← semantic compression required
end

PASS AClaudeImplementation:
    sutra: "← compress A. Claude Implementation"
    gloss:
        ← semantic compression required
end

PASS BChatgptImplementation:
    sutra: "← compress B. ChatGPT Implementation"
    gloss:
        ← semantic compression required
end

PASS CCliImplementation:
    sutra: "← compress C. CLI Implementation"
    gloss:
        ← semantic compression required
end

PASS PartViiOperationalChecklists:
    sutra: "← compress PART VII: OPERATIONAL CHECKLISTS"
    gloss:
        ← semantic compression required
end

PASS ANewSessionChecklist:
    sutra: "← compress A. New Session Checklist"
    gloss:
        ← semantic compression required
end

PASS BThreadCulminationChecklist:
    sutra: "← compress B. Thread Culmination Checklist"
    gloss:
        ← semantic compression required
end

PASS CCorpusUpdateChecklist:
    sutra: "← compress C. Corpus Update Checklist"
    gloss:
        ← semantic compression required
end

PASS DMemoryHygieneChecklist:
    sutra: "← compress D. Memory Hygiene Checklist (Monthly)"
    gloss:
        ← semantic compression required
end

PASS PartViiiFutureEvolution:
    sutra: "← compress PART VIII: FUTURE EVOLUTION"
    gloss:
        ← semantic compression required
end

PASS AAnticipatedPlatformDevelopments:
    sutra: "← compress A. Anticipated Platform Developments"
    gloss:
        ← semantic compression required
end

NORM BArchitectureAdaptationPrinciples:
    sutra: "← compress B. Architecture Adaptation Principles"
    gloss:
        ← semantic compression required
end

PASS CMeasurementAndIteration:
    sutra: "← compress C. Measurement and Iteration"
    gloss:
        ← semantic compression required
end

PASS AppendixAClaudemdTemplate:
    sutra: "← compress APPENDIX A: CLAUDE.md TEMPLATE"
    gloss:
        ← semantic compression required
end

PASS ProjectContextProjectName:
    sutra: "← compress Project Context: [PROJECT NAME]"
    gloss:
        ← semantic compression required
end

PASS Identity:
    sutra: "← compress Identity"
    gloss:
        ← semantic compression required
end

PASS CanonicalKnowledge:
    sutra: "← compress Canonical Knowledge"
    gloss:
        ← semantic compression required
end

PASS OperationalParameters:
    sutra: "← compress Operational Parameters"
    gloss:
        ← semantic compression required
end

PASS CurrentFocus:
    sutra: "← compress Current Focus"
    gloss:
        ← semantic compression required
end

PASS MemoryNotes:
    sutra: "← compress Memory Notes"
    gloss:
        ← semantic compression required
end

PASS AppendixBThreadCulmination:
    sutra: "← compress APPENDIX B: THREAD CULMINATION TEMPLATE"
    gloss:
        ← semantic compression required
end

PASS ThreadCulminationDateTopic:
    sutra: "← compress Thread Culmination: [DATE] - [TOPIC]"
    gloss:
        ← semantic compression required
end

PASS TrajectorySummary:
    sutra: "← compress Trajectory Summary"
    gloss:
        ← semantic compression required
end

PASS KeyInsightsGenerated:
    sutra: "← compress Key Insights Generated"
    gloss:
        ← semantic compression required
end

PASS UnresolvedQuestions:
    sutra: "← compress Unresolved Questions"
    gloss:
        ← semantic compression required
end

PASS CanonicalCandidates:
    sutra: "← compress Canonical Candidates"
    gloss:
        ← semantic compression required
end

PASS ContinuationContext:
    sutra: "← compress Continuation Context"
    gloss:
        ← semantic compression required
end

PASS NextActions:
    sutra: "← compress Next Actions"
    gloss:
        ← semantic compression required
end

PASS TeleologicalAnalysis:
    sutra: "← compress TELEOLOGICAL ANALYSIS"
    gloss:
        ← semantic compression required
end

PASS FormerlyCanon25010MemoryArchitecture:
    sutra: "← compress Formerly CANON-25010: Memory Architecture Teleology"
    gloss:
        ← semantic compression required
end

PASS ITheMemoryProblem:
    sutra: "← compress I. The Memory Problem"
    gloss:
        ← semantic compression required
end

PASS IiClaudeMemoryArchitecture:
    sutra: "← compress II. Claude Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1GlobalSystem:
    sutra: "← compress Layer 1: Global System Preferences (Account-Level)"
    gloss:
        ← semantic compression required
end

PASS Layer2ProjectspecificMemory:
    sutra: "← compress Layer 2: Project-Specific Memory (Project-Level)"
    gloss:
        ← semantic compression required
end

PASS Layer3ConversationMemory:
    sutra: "← compress Layer 3: Conversation Memory (Ephemeral Session Context)"
    gloss:
        ← semantic compression required
end

PASS Layer4PastConversations:
    sutra: "← compress Layer 4: Past Conversations (Cross-Thread Reference)"
    gloss:
        ← semantic compression required
end

PASS IiiChatgptMemoryArchitecture:
    sutra: "← compress III. ChatGPT Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1GlobalCustom:
    sutra: "← compress Layer 1: Global Custom Instructions (Account-Level)"
    gloss:
        ← semantic compression required
end

PASS Layer2GlobalMemory:
    sutra: "← compress Layer 2: Global Memory (Account-Level, Passive Learning)"
    gloss:
        ← semantic compression required
end

PASS Layer3ProjectspecificConfiguration:
    sutra: "← compress Layer 3: Project-Specific Configuration (Project-Level)"
    gloss:
        ← semantic compression required
end

PASS Layer4ConversationContext:
    sutra: "← compress Layer 4: Conversation Context (Ephemeral Session)"
    gloss:
        ← semantic compression required
end

PASS Layer5CanvasWorkspace:
    sutra: "← compress Layer 5: Canvas (Workspace Artifacts)"
    gloss:
        ← semantic compression required
end

PASS IvGeminiMemoryArchitecture:
    sutra: "← compress IV. Gemini Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1SavedInfo:
    sutra: "← compress Layer 1: Saved Info (Global Personal Preferences)"
    gloss:
        ← semantic compression required
end

PASS Layer2PersonalIntelligence:
    sutra: "← compress Layer 2: Personal Intelligence (Connected App Context)"
    gloss:
        ← semantic compression required
end

PASS Layer3GemsCustom:
    sutra: "← compress Layer 3: Gems (Custom AI Personas)"
    gloss:
        ← semantic compression required
end

PASS Layer4ConversationContext:
    sutra: "← compress Layer 4: Conversation Context (Ephemeral Session)"
    gloss:
        ← semantic compression required
end

PASS Layer5NotebooklmIntegration:
    sutra: "← compress Layer 5: NotebookLM Integration"
    gloss:
        ← semantic compression required
end

PASS VGeminiCliMemory:
    sutra: "← compress V. Gemini CLI Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1LocalConfiguration:
    sutra: "← compress Layer 1: Local Configuration Files"
    gloss:
        ← semantic compression required
end

PASS Layer2InvocationContext:
    sutra: "← compress Layer 2: Invocation Context (Per-Command)"
    gloss:
        ← semantic compression required
end

PASS Layer3NoPersistent:
    sutra: "← compress Layer 3: No Persistent Memory"
    gloss:
        ← semantic compression required
end

PASS ViClaudeCodeMemory:
    sutra: "← compress VI. Claude Code Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1UserGlobal:
    sutra: "← compress Layer 1: User Global Memory (~/.claude/CLAUDE.md)"
    gloss:
        ← semantic compression required
end

PASS Layer2ProjectMemory:
    sutra: "← compress Layer 2: Project Memory (./CLAUDE.md)"
    gloss:
        ← semantic compression required
end

NORM Layer3ProjectRules:
    sutra: "← compress Layer 3: Project Rules (.claude/rules/*.md)"
    gloss:
        ← semantic compression required
end

PASS Layer4ProjectLocal:
    sutra: "← compress Layer 4: Project Local Memory (./CLAUDE.local.md)"
    gloss:
        ← semantic compression required
end

PASS Layer5SubdirectoryMemory:
    sutra: "← compress Layer 5: Subdirectory Memory (Nested CLAUDE.md)"
    gloss:
        ← semantic compression required
end

PASS Layer6SessionMemory:
    sutra: "← compress Layer 6: Session Memory (Ephemeral)"
    gloss:
        ← semantic compression required
end

PASS Layer7TeleportWeb:
    sutra: "← compress Layer 7: Teleport (Web ↔ Terminal Bridge)"
    gloss:
        ← semantic compression required
end

PASS ViiPerplexityMemoryArchitecture:
    sutra: "← compress VII. Perplexity Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1UserProfile:
    sutra: "← compress Layer 1: User Profile"
    gloss:
        ← semantic compression required
end

PASS Layer2ThreadContext:
    sutra: "← compress Layer 2: Thread Context"
    gloss:
        ← semantic compression required
end

PASS ViiiGrokMemoryArchitecture:
    sutra: "← compress VIII. Grok Memory Architecture"
    gloss:
        ← semantic compression required
end

PASS Layer1XAccount:
    sutra: "← compress Layer 1: X Account Context (Implicit)"
    gloss:
        ← semantic compression required
end

PASS Layer2ThreadContext:
    sutra: "← compress Layer 2: Thread Context"
    gloss:
        ← semantic compression required
end

PASS Layer3XFirehose:
    sutra: "← compress Layer 3: X Firehose (Real-Time)"
    gloss:
        ← semantic compression required
end

PASS IxSurfacespecificMemoryBehavior:
    sutra: "← compress IX. Surface-Specific Memory Behavior"
    gloss:
        ← semantic compression required
end

PASS WebBrowserSurface:
    sutra: "← compress Web Browser Surface"
    gloss:
        ← semantic compression required
end

PASS DesktopAppSurface:
    sutra: "← compress Desktop App Surface"
    gloss:
        ← semantic compression required
end

PASS CliSurface:
    sutra: "← compress CLI Surface"
    gloss:
        ← semantic compression required
end

PASS MobileSurface:
    sutra: "← compress Mobile Surface"
    gloss:
        ← semantic compression required
end

PROC XMemoryHandoffProtocol:
    sutra: "← compress X. Memory Handoff Protocol Rationale"
    gloss:
        ← semantic compression required
end

PASS StateFingerprints:
    sutra: "← compress State Fingerprints"
    gloss:
        ← semantic compression required
end

PASS PlatformspecificCaching:
    sutra: "← compress Platform-Specific Caching"
    gloss:
        ← semantic compression required
end

PASS RepositoryAsGroundTruth:
    sutra: "← compress Repository as Ground Truth"
    gloss:
        ← semantic compression required
end

PASS XiForbiddenMemoryPatterns:
    sutra: "← compress XI. Forbidden Memory Patterns"
    gloss:
        ← semantic compression required
end

PASS DoNotRelyOn:
    sutra: "← compress Do NOT rely on ChatGPT Global Memory for project work"
    gloss:
        ← semantic compression required
end

PASS DoNotExpectCrossgem:
    sutra: "← compress Do NOT expect cross-Gem memory in Gemini"
    gloss:
        ← semantic compression required
end

PASS DoNotAssumeGemini:
    sutra: "← compress Do NOT assume Gemini CLI remembers previous invocations"
    gloss:
        ← semantic compression required
end

PASS DoNotUseClaude:
    sutra: "← compress Do NOT use Claude Web threads as long-term memory"
    gloss:
        ← semantic compression required
end

PASS DoNotRelyOn:
    sutra: "← compress Do NOT rely on mobile for memory configuration"
    gloss:
        ← semantic compression required
end

PASS DoNotExpectPerplexity:
    sutra: "← compress Do NOT expect Perplexity or Grok to remember anything"
    gloss:
        ← semantic compression required
end

PASS XiiTheUnifiedMemory:
    sutra: "← compress XII. The Unified Memory Rationale"
    gloss:
        ← semantic compression required
end

PASS Crossreferences:
    sutra: "← compress CROSS-REFERENCES"
    gloss:
        ← semantic compression required
end
