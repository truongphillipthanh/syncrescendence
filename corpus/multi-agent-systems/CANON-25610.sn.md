---
id: [[CANON-25610]]
name: Diviner Prompting Formula
tier: lattice
celestial_type: planetary
volatility_band: moderate
sn_version: 2.0
parent: CANON-25600
requires: ['CANON-00016', 'CANON-25600']
entities: ['Cognitive Launching Pads (PROTO)', 'Symmetry-Breaking Fields (CON)', 'Micro-Falsifiability Covenant (PROTO)', 'Negative Space Hardening (PROTO)', 'All-Sciences Palette (PROTO)']
original_words: 1093
original_lines: 126
---

# Κ-25610: Diviner Prompting Formula (SN)

PASS Canon25610TheDivinerPrompting:
    sutra: "← compress CANON-25610: THE DIVINER PROMPTING FORMULA"
    gloss:
        ← semantic compression required
end

PASS StructuralElementsForMultiagent:
    sutra: "← compress Structural Elements for Multi-Agent Dialectical Synthesis"
    gloss:
        ← semantic compression required
end

PASS Purpose:
    sutra: "← compress PURPOSE"
    gloss:
        ← semantic compression required
end

PASS The8NonnegotiableElements:
    sutra: "← compress THE 8 NON-NEGOTIABLE ELEMENTS"
    gloss:
        ← semantic compression required
end

PASS 1CognitiveLaunchingPads:
    sutra: "← compress 1. Cognitive Launching Pads"
    gloss:
        ← semantic compression required
end

PASS 2AllsciencesPalette:
    sutra: "← compress 2. All-Sciences Palette"
    gloss:
        ← semantic compression required
end

NORM 3LatticeLanguageConstraint:
    sutra: "← compress 3. Lattice Language Constraint (CANON-00016)"
    gloss:
        ← semantic compression required
end

PASS 4NegativeSpaceHardening:
    sutra: "← compress 4. Negative Space Hardening"
    gloss:
        ← semantic compression required
end

PASS 5MicrofalsifiabilityCovenant:
    sutra: "← compress 5. Micro-Falsifiability Covenant"
    gloss:
        ← semantic compression required
end

PASS 6DivinersOwnLineage:
    sutra: "← compress 6. Diviner's Own Lineage"
    gloss:
        ← semantic compression required
end

PASS 7StatelessBootstrapLine:
    sutra: "← compress 7. Stateless Bootstrap Line"
    gloss:
        ← semantic compression required
end

PASS 8OraclesFullThesis:
    sutra: "← compress 8. Oracle's Full Thesis Embedded"
    gloss:
        ← semantic compression required
end

PASS WhatNotToDo:
    sutra: "← compress WHAT NOT TO DO (FAILURES CATALOGED)"
    gloss:
        ← semantic compression required
end

PASS StructuralNotes:
    sutra: "← compress STRUCTURAL NOTES"
    gloss:
        ← semantic compression required
end

PASS TemplateSkeleton:
    sutra: "← compress TEMPLATE SKELETON"
    gloss:
        ← semantic compression required
end

PASS DivinerInstrumentCcnPass:
    sutra: "← compress DIVINER [INSTRUMENT] — CC{N} PASS {X} OF {Y}"
    gloss:
        ← semantic compression required
end

PASS YourRole:
    sutra: "← compress Your Role"
    gloss:
        ← semantic compression required
end

PASS TheExchangeYouAre:
    sutra: "← compress THE EXCHANGE YOU ARE ENTERING"
    gloss:
        ← semantic compression required
end

PASS Task1ClarescenceRepo:
    sutra: "← compress TASK 1: [CLARESCENCE / REPO TRAVERSAL / OTHER GROUNDING]"
    gloss:
        ← semantic compression required
end

PASS Task2YourAllsciences:
    sutra: "← compress TASK 2: YOUR ALL-SCIENCES NOVEL SYNTHESIS"
    gloss:
        ← semantic compression required
end

PASS QuestionsWithCognitiveLaunching:
    sutra: "← compress Questions With Cognitive Launching Pads"
    gloss:
        ← semantic compression required
end

PASS YourLineage:
    sutra: "← compress YOUR LINEAGE"
    gloss:
        ← semantic compression required
end

PASS OutputFormat:
    sutra: "← compress OUTPUT FORMAT"
    gloss:
        ← semantic compression required
end

NORM Constraints:
    sutra: "← compress CONSTRAINTS"
    gloss:
        ← semantic compression required
end
