---
id: [[CANON-20010]]
name: Dimensional Coordinators
tier: lattice
celestial_type: satellite
volatility_band: moderate
sn_version: 2.0
parent: CANON-20000
requires: ['CANON-20000']
entities: ['Reality Coordinator (AGT)', 'Imaginality Coordinator (AGT)', 'Potentiality Coordinator (AGT)', 'Temporality Coordinator (AGT)', 'Practicality Coordinator (AGT)', 'Actuality Coordinator (AGT)', 'Consequentiality Coordinator (AGT)', 'Meta-Governor (AGT)', 'Seven Pulses Dashboard (TOOL)', 'Cross-Layer Integration Matrices (STR)']
original_words: 1238
original_lines: 287
---

# Κ-20010: Dimensional Coordinators (SN)

PASS Canon20010DimensionalCoordinators:
    sutra: "← compress CANON-20010: DIMENSIONAL COORDINATORS"
    gloss:
        ← semantic compression required
end

PASS CognitivePalaceSatelliteThe:
    sutra: "← compress Cognitive Palace Satellite — The Orchestrating Intelligence"
    gloss:
        ← semantic compression required
end

PASS Purpose:
    sutra: "← compress PURPOSE"
    gloss:
        ← semantic compression required
end

PASS TheSevenCoordinators:
    sutra: "← compress THE SEVEN COORDINATORS"
    gloss:
        ← semantic compression required
end

PASS 00RealityCoordinatorFoundation:
    sutra: "← compress 00: Reality Coordinator (Foundation)"
    gloss:
        ← semantic compression required
end

PASS 10ImaginalityCoordinatorVision:
    sutra: "← compress 10: Imaginality Coordinator (Vision)"
    gloss:
        ← semantic compression required
end

PASS 20PotentialityCoordinatorCapacity:
    sutra: "← compress 20: Potentiality Coordinator (Capacity)"
    gloss:
        ← semantic compression required
end

PASS 30TemporalityCoordinatorTime:
    sutra: "← compress 30: Temporality Coordinator (Time)"
    gloss:
        ← semantic compression required
end

PASS 40PracticalityCoordinatorAction:
    sutra: "← compress 40: Practicality Coordinator (Action)"
    gloss:
        ← semantic compression required
end

PASS 50ActualityCoordinatorPresence:
    sutra: "← compress 50: Actuality Coordinator (Presence)"
    gloss:
        ← semantic compression required
end

PASS 60ConsequentialityCoordinatorImpact:
    sutra: "← compress 60: Consequentiality Coordinator (Impact)"
    gloss:
        ← semantic compression required
end

PASS MetacoordinationTheGovernor:
    sutra: "← compress META-COORDINATION: THE GOVERNOR"
    gloss:
        ← semantic compression required
end

PASS SevenPulsesDashboard:
    sutra: "← compress SEVEN PULSES DASHBOARD"
    gloss:
        ← semantic compression required
end

PASS CrosslayerIntegration:
    sutra: "← compress CROSS-LAYER INTEGRATION"
    gloss:
        ← semantic compression required
end

PASS OperationalModes:
    sutra: "← compress OPERATIONAL MODES"
    gloss:
        ← semantic compression required
end

PASS InvisibleModeDefault:
    sutra: "← compress Invisible Mode (Default)"
    gloss:
        ← semantic compression required
end

PASS AdvisoryModeWhenRequested:
    sutra: "← compress Advisory Mode (When Requested)"
    gloss:
        ← semantic compression required
end

PASS NaturalLanguageExpression:
    sutra: "← compress NATURAL LANGUAGE EXPRESSION"
    gloss:
        ← semantic compression required
end

PASS SuccessIndicators:
    sutra: "← compress SUCCESS INDICATORS"
    gloss:
        ← semantic compression required
end

PASS EvolutionPathway:
    sutra: "← compress EVOLUTION PATHWAY"
    gloss:
        ← semantic compression required
end
