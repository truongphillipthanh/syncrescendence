---
id: [[CANON-00002]]
name: Intellectual Lineage
tier: cosmos
celestial_type: root
volatility_band: permanent
sn_version: 2.0
parent: CANON-00000
entities: ['Extended Mind Thesis (CON)', 'Strange Loops (CON)', 'Meaning Crisis (CON)', 'Adult Development Theory (CON)', 'Fourth Turning (CON)', 'Metamodernism (CON)', 'Integral Theory (CON)', 'Systems Thinking (CON)', 'Recurse Theory of Consciousness (CON)', 'Synthesis Principle (CON)']
original_words: 1950
original_lines: 320
---

# Κ-00002: Intellectual Lineage (SN)

PASS Canon00002TheIntellectualLineage:
    sutra: "← compress CANON-00002: THE INTELLECTUAL LINEAGE"
    gloss:
        ← semantic compression required
end

PASS WhereSyncrescendenceComesFrom:
    sutra: "← compress Where Syncrescendence Comes From"
    gloss:
        ← semantic compression required
end

PASS CelestialNavigation:
    sutra: "← compress CELESTIAL NAVIGATION"
    gloss:
        ← semantic compression required
end

PASS TheRoots:
    sutra: "← compress THE ROOTS"
    gloss:
        ← semantic compression required
end

PASS PartIThePrimary:
    sutra: "← compress PART I: THE PRIMARY TRIBUTARIES"
    gloss:
        ← semantic compression required
end

PASS 1TheExtendedMind:
    sutra: "← compress 1. The Extended Mind Thesis"
    gloss:
        ← semantic compression required
end

PASS 2StrangeLoopsAnd:
    sutra: "← compress 2. Strange Loops and Self-Reference"
    gloss:
        ← semantic compression required
end

PASS 3TheMeaningCrisis:
    sutra: "← compress 3. The Meaning Crisis"
    gloss:
        ← semantic compression required
end

PASS 4AdultDevelopmentTheory:
    sutra: "← compress 4. Adult Development Theory"
    gloss:
        ← semantic compression required
end

PASS 5TheFourthTurning:
    sutra: "← compress 5. The Fourth Turning"
    gloss:
        ← semantic compression required
end

PASS 6Metamodernism:
    sutra: "← compress 6. Metamodernism"
    gloss:
        ← semantic compression required
end

PASS 7IntegralTheory:
    sutra: "← compress 7. Integral Theory"
    gloss:
        ← semantic compression required
end

PASS 8SystemsThinking:
    sutra: "← compress 8. Systems Thinking"
    gloss:
        ← semantic compression required
end

PASS 9ContemplativeTraditions:
    sutra: "← compress 9. Contemplative Traditions"
    gloss:
        ← semantic compression required
end

PASS PartIiSecondaryTributaries:
    sutra: "← compress PART II: SECONDARY TRIBUTARIES"
    gloss:
        ← semantic compression required
end

PASS 10Metahumanism:
    sutra: "← compress 10. Metahumanism"
    gloss:
        ← semantic compression required
end

PASS 11MorphologicalAnalysis:
    sutra: "← compress 11. Morphological Analysis"
    gloss:
        ← semantic compression required
end

PASS 12FlowPsychology:
    sutra: "← compress 12. Flow Psychology"
    gloss:
        ← semantic compression required
end

PASS 13CausationContinuumTheory:
    sutra: "← compress 13. Causation Continuum Theory"
    gloss:
        ← semantic compression required
end

NORM PartIiiTheSynthesis:
    sutra: "← compress PART III: THE SYNTHESIS PRINCIPLE"
    gloss:
        ← semantic compression required
end

PASS PartIvGratitudeAnd:
    sutra: "← compress PART IV: GRATITUDE AND LIMITATION"
    gloss:
        ← semantic compression required
end

PASS TheDebt:
    sutra: "← compress The Debt"
    gloss:
        ← semantic compression required
end

PASS TheLimitation:
    sutra: "← compress The Limitation"
    gloss:
        ← semantic compression required
end

PASS VersionHistory:
    sutra: "← compress VERSION HISTORY"
    gloss:
        ← semantic compression required
end
