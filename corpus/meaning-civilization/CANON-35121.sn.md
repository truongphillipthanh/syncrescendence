---
id: [[CANON-35121]]
name: Neurodivergent Patterns
tier: chain
chain: wisdom
celestial_type: satellite
volatility_band: moderate
sn_version: 2.0
parent: CANON-35120
requires: ['CANON-35120', 'CANON-35100', 'CANON-35000']
entities: ['Neurodivergent Patterns (STR)', 'AuDHD Implementation Patterns (PROTO)', 'Executive Function Protocols (PROTO)', 'Sensory Processing Adaptations (PROTO)', 'Energy Management Patterns (PROTO)', 'Double-Masking Dynamics (CON)']
original_words: 1454
original_lines: 298
---

# Κ-35121: Neurodivergent Patterns (SN)

PASS Canon35121NeurodivergentPatterns:
    sutra: "← compress CANON-35121: Neurodivergent Patterns"
    gloss:
        ← semantic compression required
end

PASS AudhdImplementationSatellite:
    sutra: "← compress AuDHD Implementation Satellite"
    gloss:
        ← semantic compression required
end

PASS Purpose:
    sutra: "← compress Purpose"
    gloss:
        ← semantic compression required
end

PASS 1ExecutiveFunctionMechanisms:
    sutra: "← compress 1. Executive Function Mechanisms"
    gloss:
        ← semantic compression required
end

PASS TheAdditiveComorbidityModel:
    sutra: "← compress The Additive Comorbidity Model"
    gloss:
        ← semantic compression required
end

PASS PracticalImplications:
    sutra: "← compress Practical Implications"
    gloss:
        ← semantic compression required
end

PASS 2TheKnowingdoingGap:
    sutra: "← compress 2. The Knowing-Doing Gap"
    gloss:
        ← semantic compression required
end

TERM Definition:
    sutra: "← compress Definition"
    gloss:
        ← semantic compression required
end

PASS TheSpikyCognitiveProfile:
    sutra: "← compress The Spiky Cognitive Profile"
    gloss:
        ← semantic compression required
end

PASS CompensationStrategies:
    sutra: "← compress Compensation Strategies"
    gloss:
        ← semantic compression required
end

PROC 3SensoryProcessingIntegration:
    sutra: "← compress 3. Sensory Processing Integration"
    gloss:
        ← semantic compression required
end

PASS TheSensorySeesaw:
    sutra: "← compress The Sensory Seesaw"
    gloss:
        ← semantic compression required
end

PASS PracticalApplications:
    sutra: "← compress Practical Applications"
    gloss:
        ← semantic compression required
end

PASS InteroceptionDeficits:
    sutra: "← compress Interoception Deficits"
    gloss:
        ← semantic compression required
end

PROC 4EnergyManagementProtocols:
    sutra: "← compress 4. Energy Management Protocols"
    gloss:
        ← semantic compression required
end

PASS TheBrokenFuelGauge:
    sutra: "← compress The Broken Fuel Gauge Model"
    gloss:
        ← semantic compression required
end

PASS TheCompetencyTrap:
    sutra: "← compress The Competency Trap"
    gloss:
        ← semantic compression required
end

NORM EnergyManagementPrinciples:
    sutra: "← compress Energy Management Principles"
    gloss:
        ← semantic compression required
end

PASS 5RejectionSensitiveDysphoria:
    sutra: "← compress 5. Rejection Sensitive Dysphoria (RSD)"
    gloss:
        ← semantic compression required
end

PASS NeurologicalBasis:
    sutra: "← compress Neurological Basis"
    gloss:
        ← semantic compression required
end

PASS Manifestations:
    sutra: "← compress Manifestations"
    gloss:
        ← semantic compression required
end

PASS CopingPatterns:
    sutra: "← compress Coping Patterns"
    gloss:
        ← semantic compression required
end

PASS RsdAndMasking:
    sutra: "← compress RSD and Masking"
    gloss:
        ← semantic compression required
end

PASS 6DoublemaskingDynamics:
    sutra: "← compress 6. Double-Masking Dynamics"
    gloss:
        ← semantic compression required
end

PASS TheLayeredPerformance:
    sutra: "← compress The Layered Performance"
    gloss:
        ← semantic compression required
end

PASS LongtermConsequences:
    sutra: "← compress Long-Term Consequences"
    gloss:
        ← semantic compression required
end

PASS TheCompetencyTrapRevisited:
    sutra: "← compress The Competency Trap (Revisited)"
    gloss:
        ← semantic compression required
end

PASS 7JusticeSensitivity:
    sutra: "← compress 7. Justice Sensitivity"
    gloss:
        ← semantic compression required
end

PASS NeurobiologicalBasis:
    sutra: "← compress Neurobiological Basis"
    gloss:
        ← semantic compression required
end

PASS FourDomains:
    sutra: "← compress Four Domains"
    gloss:
        ← semantic compression required
end

PASS Implications:
    sutra: "← compress Implications"
    gloss:
        ← semantic compression required
end

PASS 8StimmingAsRegulation:
    sutra: "← compress 8. Stimming as Regulation Tool"
    gloss:
        ← semantic compression required
end

PASS DualRegulatoryFunction:
    sutra: "← compress Dual Regulatory Function"
    gloss:
        ← semantic compression required
end

PASS CommonStims:
    sutra: "← compress Common Stims"
    gloss:
        ← semantic compression required
end

PASS PracticalAccommodation:
    sutra: "← compress Practical Accommodation"
    gloss:
        ← semantic compression required
end

PASS IntegrationWithSyncrescendentPractice:
    sutra: "← compress Integration with Syncrescendent Practice"
    gloss:
        ← semantic compression required
end

PASS SevenPulsesAdaptation:
    sutra: "← compress Seven Pulses Adaptation"
    gloss:
        ← semantic compression required
end

PASS DailyPracticeConsiderations:
    sutra: "← compress Daily Practice Considerations"
    gloss:
        ← semantic compression required
end

PROC CrisisProtocolAddition:
    sutra: "← compress Crisis Protocol Addition"
    gloss:
        ← semantic compression required
end

PASS Summary:
    sutra: "← compress Summary"
    gloss:
        ← semantic compression required
end
