---
id: [[CANON-31121]]
name: Tone Library Taxonomy
tier: chain
chain: information
celestial_type: satellite
volatility_band: moderate
sn_version: 2.0
parent: CANON-31120
requires: ['CANON-31120', 'CANON-31000', 'CANON-00000', 'CANON-00006', 'CANON-30000']
siblings: ['CANON-31122']
entities: ['Rhetorical Function Taxonomy (STR)', 'Stylistic Form Taxonomy (STR)', 'Cadence Pattern Taxonomy (STR)', 'Diction Pattern Taxonomy (STR)', 'Guardrail Taxonomy (STR)', 'Evidence Type Taxonomy (STR)', 'CTA Pattern Taxonomy (STR)', 'Scaffold Element Taxonomy (STR)', 'Family Taxonomy (STR)', 'Recipient Archetype Taxonomy (STR)', 'Domain Taxonomy (STR)']
original_words: 2314
original_lines: 502
---

# Κ-31121: Tone Library Taxonomy (SN)

PASS ToneLibraryCompleteTaxonomy:
    sutra: "← compress TONE LIBRARY: Complete Taxonomy Reference"
    gloss:
        ← semantic compression required
end

PASS Purpose:
    sutra: "← compress Purpose"
    gloss:
        ← semantic compression required
end

PASS CelestialNavigation:
    sutra: "← compress CELESTIAL NAVIGATION"
    gloss:
        ← semantic compression required
end

PASS IRhetoricalFunction10:
    sutra: "← compress I. RHETORICAL FUNCTION (10 Functions)"
    gloss:
        ← semantic compression required
end

PASS 1InformDescribe:
    sutra: "← compress 1. Inform & Describe"
    gloss:
        ← semantic compression required
end

PASS 2ExplainInterpret:
    sutra: "← compress 2. Explain & Interpret"
    gloss:
        ← semantic compression required
end

PASS 3InstructEnable:
    sutra: "← compress 3. Instruct & Enable"
    gloss:
        ← semantic compression required
end

PASS 4EvaluateAppraise:
    sutra: "← compress 4. Evaluate & Appraise"
    gloss:
        ← semantic compression required
end

PASS 5ArguePersuade:
    sutra: "← compress 5. Argue & Persuade"
    gloss:
        ← semantic compression required
end

PASS 6DecideRecommend:
    sutra: "← compress 6. Decide & Recommend"
    gloss:
        ← semantic compression required
end

PASS 7MobilizeOrganize:
    sutra: "← compress 7. Mobilize & Organize"
    gloss:
        ← semantic compression required
end

PASS 8ConsoleRepair:
    sutra: "← compress 8. Console & Repair"
    gloss:
        ← semantic compression required
end

PASS 9SensemakeReframe:
    sutra: "← compress 9. Sense-make & Reframe"
    gloss:
        ← semantic compression required
end

PASS 10InvestigateForensic:
    sutra: "← compress 10. Investigate & Forensic"
    gloss:
        ← semantic compression required
end

PASS IiStylisticForm15:
    sutra: "← compress II. STYLISTIC FORM (15 Styles)"
    gloss:
        ← semantic compression required
end

PASS NarrativeStoryFamily:
    sutra: "← compress Narrative & Story Family"
    gloss:
        ← semantic compression required
end

PASS ExplanatoryMapmakingFamily:
    sutra: "← compress Explanatory & Map-Making Family"
    gloss:
        ← semantic compression required
end

PASS InstructionalProceduralFamily:
    sutra: "← compress Instructional & Procedural Family"
    gloss:
        ← semantic compression required
end

PASS IiiCadencePattern4:
    sutra: "← compress III. CADENCE PATTERN (4 Bands)"
    gloss:
        ← semantic compression required
end

PASS 1Staccato812Words:
    sutra: "← compress 1. Staccato (8–12 words per sentence)"
    gloss:
        ← semantic compression required
end

PASS 2Compact1216Words:
    sutra: "← compress 2. Compact (12–16 words per sentence)"
    gloss:
        ← semantic compression required
end

PASS 3Measured1620Words:
    sutra: "← compress 3. Measured (16–20 words per sentence)"
    gloss:
        ← semantic compression required
end

PASS 4Longline2028Words:
    sutra: "← compress 4. Long-line (20–28 words per sentence)"
    gloss:
        ← semantic compression required
end

PASS IvDictionPattern4:
    sutra: "← compress IV. DICTION PATTERN (4 Registers)"
    gloss:
        ← semantic compression required
end

PASS 1PlainEnglish:
    sutra: "← compress 1. Plain English"
    gloss:
        ← semantic compression required
end

PASS 2TechnicalPrecise:
    sutra: "← compress 2. Technical Precise"
    gloss:
        ← semantic compression required
end

PASS 3ElevatedClean:
    sutra: "← compress 3. Elevated Clean"
    gloss:
        ← semantic compression required
end

PASS 4SecondpersonDirect:
    sutra: "← compress 4. Second-person Direct"
    gloss:
        ← semantic compression required
end

NORM VGuardrail5Constraints:
    sutra: "← compress V. GUARDRAIL (5 Constraints)"
    gloss:
        ← semantic compression required
end

PASS 1SteelmanOpposingView:
    sutra: "← compress 1. Steelman Opposing View"
    gloss:
        ← semantic compression required
end

PASS 2BoundedClaims:
    sutra: "← compress 2. Bounded Claims"
    gloss:
        ← semantic compression required
end

PASS 3SourceAttribution:
    sutra: "← compress 3. Source Attribution"
    gloss:
        ← semantic compression required
end

PASS 4HazardNotice:
    sutra: "← compress 4. Hazard Notice"
    gloss:
        ← semantic compression required
end

PASS 5PrivacysafeFraming:
    sutra: "← compress 5. Privacy-Safe Framing"
    gloss:
        ← semantic compression required
end

PASS ViEvidenceType5:
    sutra: "← compress VI. EVIDENCE TYPE (5 Modes)"
    gloss:
        ← semantic compression required
end

PASS 1Anecdote:
    sutra: "← compress 1. Anecdote"
    gloss:
        ← semantic compression required
end

PASS 2Case:
    sutra: "← compress 2. Case"
    gloss:
        ← semantic compression required
end

PASS 3Data:
    sutra: "← compress 3. Data"
    gloss:
        ← semantic compression required
end

PASS 4Mechanism:
    sutra: "← compress 4. Mechanism"
    gloss:
        ← semantic compression required
end

PASS 5Demonstration:
    sutra: "← compress 5. Demonstration"
    gloss:
        ← semantic compression required
end

PASS ViiCtaPattern5:
    sutra: "← compress VII. CTA PATTERN (5 Actions)"
    gloss:
        ← semantic compression required
end

PASS 1Checklist:
    sutra: "← compress 1. Checklist"
    gloss:
        ← semantic compression required
end

PASS 2Exercise:
    sutra: "← compress 2. Exercise"
    gloss:
        ← semantic compression required
end

PASS 3QuestionSet:
    sutra: "← compress 3. Question Set"
    gloss:
        ← semantic compression required
end

PASS 4Decision:
    sutra: "← compress 4. Decision"
    gloss:
        ← semantic compression required
end

PASS 5ReportBack:
    sutra: "← compress 5. Report Back"
    gloss:
        ← semantic compression required
end

PASS ViiiScaffoldElement7:
    sutra: "← compress VIII. SCAFFOLD ELEMENT (7 Structures)"
    gloss:
        ← semantic compression required
end

PASS 1HeadingsH2h3:
    sutra: "← compress 1. Headings (H2/H3)"
    gloss:
        ← semantic compression required
end

PROC 2NumberedSteps:
    sutra: "← compress 2. Numbered Steps"
    gloss:
        ← semantic compression required
end

PASS 3BulletedLists:
    sutra: "← compress 3. Bulleted Lists"
    gloss:
        ← semantic compression required
end

PASS 4Callouts:
    sutra: "← compress 4. Callouts"
    gloss:
        ← semantic compression required
end

PASS 5Sidebar:
    sutra: "← compress 5. Sidebar"
    gloss:
        ← semantic compression required
end

PASS 6FaqBlock:
    sutra: "← compress 6. FAQ Block"
    gloss:
        ← semantic compression required
end

PASS 7TimelineTable:
    sutra: "← compress 7. Timeline Table"
    gloss:
        ← semantic compression required
end

PASS IxFamily10Flavors:
    sutra: "← compress IX. FAMILY (10 Flavors)"
    gloss:
        ← semantic compression required
end

PASS XRecipientArchetype10:
    sutra: "← compress X. RECIPIENT ARCHETYPE (10 Personas)"
    gloss:
        ← semantic compression required
end

PROC 1FirststepNovice:
    sutra: "← compress 1. First-Step Novice"
    gloss:
        ← semantic compression required
end

PASS 2CredentialseekerStudent:
    sutra: "← compress 2. Credential-Seeker Student"
    gloss:
        ← semantic compression required
end

PASS 3SelfdirectedGeneralist:
    sutra: "← compress 3. Self-Directed Generalist"
    gloss:
        ← semantic compression required
end

PASS 4Meaningseeker:
    sutra: "← compress 4. Meaning-Seeker"
    gloss:
        ← semantic compression required
end

PROC 5WorkflowOptimizerPractitioner:
    sutra: "← compress 5. Workflow Optimizer (Practitioner)"
    gloss:
        ← semantic compression required
end

PASS 6FrontlineOperator:
    sutra: "← compress 6. Frontline Operator"
    gloss:
        ← semantic compression required
end

PASS 7Buildermaker:
    sutra: "← compress 7. Builder/Maker"
    gloss:
        ← semantic compression required
end

PASS 8ReliabilityStewardOpsqa:
    sutra: "← compress 8. Reliability Steward (Ops/QA)"
    gloss:
        ← semantic compression required
end

PASS 9ExecutiveDecider:
    sutra: "← compress 9. Executive Decider"
    gloss:
        ← semantic compression required
end

PASS 10PortfolioEvaluatorInvestorgrants:
    sutra: "← compress 10. Portfolio Evaluator (Investor/Grants)"
    gloss:
        ← semantic compression required
end

PASS XiDomain15Contexts:
    sutra: "← compress XI. DOMAIN (15 Contexts)"
    gloss:
        ← semantic compression required
end

PROC UsageGuidance:
    sutra: "← compress Usage Guidance"
    gloss:
        ← semantic compression required
end

PASS CreatingARecipe:
    sutra: "← compress Creating a Recipe"
    gloss:
        ← semantic compression required
end

PASS AnalyzingExistingContent:
    sutra: "← compress Analyzing Existing Content"
    gloss:
        ← semantic compression required
end

PASS IntegrationPoints:
    sutra: "← compress Integration Points"
    gloss:
        ← semantic compression required
end

PROC MaintenanceProtocol:
    sutra: "← compress Maintenance Protocol"
    gloss:
        ← semantic compression required
end

PASS Crossreferences:
    sutra: "← compress CROSS-REFERENCES"
    gloss:
        ← semantic compression required
end
