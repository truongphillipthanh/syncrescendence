---
id: [[CANON-00015]]
name: Macroscopic Narratives
tier: cosmos
celestial_type: lunar
volatility_band: moderate
sn_version: 2.0
parent: CANON-00010
siblings: ['CANON-00005', 'CANON-00007', 'CANON-00012']
entities: ['Macroscopic Narratives (STR)', 'Reflexive Epoch (CON)', 'Integrative Interregnum (CON)', 'Civilizational Phase Transition (CON)', 'Application Protocol (PROTO)', 'Convergent Insights (CON)', 'Narrative Lens Families (STR)', 'Signal Qualification (PROTO)']
original_words: 4624
original_lines: 493
---

# Κ-00015: Macroscopic Narratives (SN)

PASS Canon00015MacroscopicNarratives:
    sutra: "← compress CANON-00015: MACROSCOPIC NARRATIVES"
    gloss:
        ← semantic compression required
end

PASS InterpretiveLensesForCivilizational:
    sutra: "← compress Interpretive Lenses for Civilizational Sensing"
    gloss:
        ← semantic compression required
end

PASS CelestialNavigation:
    sutra: "← compress CELESTIAL NAVIGATION"
    gloss:
        ← semantic compression required
end

PASS Purpose:
    sutra: "← compress PURPOSE"
    gloss:
        ← semantic compression required
end

TERM OverarchingIntegrativeConcepts:
    sutra: "← compress OVERARCHING INTEGRATIVE CONCEPTS"
    gloss:
        ← semantic compression required
end

PASS TheReflexiveEpoch:
    sutra: "← compress The Reflexive Epoch"
    gloss:
        ← semantic compression required
end

PASS TheIntegrativeInterregnum:
    sutra: "← compress The Integrative Interregnum"
    gloss:
        ← semantic compression required
end

PASS TheCivilizationalPhaseTransition:
    sutra: "← compress The Civilizational Phase Transition"
    gloss:
        ← semantic compression required
end

PASS IPlanetaryGeologicalPhases:
    sutra: "← compress I. PLANETARY & GEOLOGICAL PHASES"
    gloss:
        ← semantic compression required
end

PASS TheAnthropoceneEpoch:
    sutra: "← compress The Anthropocene Epoch"
    gloss:
        ← semantic compression required
end

PASS TheGreatAcceleration:
    sutra: "← compress The Great Acceleration"
    gloss:
        ← semantic compression required
end

PASS PlanetaryPhaseShift:
    sutra: "← compress Planetary Phase Shift"
    gloss:
        ← semantic compression required
end

PASS TheEndOfThe:
    sutra: "← compress The End of the Holocene"
    gloss:
        ← semantic compression required
end

PASS IiSystemsComplexityPhases:
    sutra: "← compress II. SYSTEMS & COMPLEXITY PHASES"
    gloss:
        ← semantic compression required
end

PASS TheMetacrisis:
    sutra: "← compress The Metacrisis"
    gloss:
        ← semantic compression required
end

PASS ThePolycrisisPermacrisis:
    sutra: "← compress The Polycrisis / Permacrisis"
    gloss:
        ← semantic compression required
end

PASS AdaptiveCycleAndCivilizational:
    sutra: "← compress Adaptive Cycle and Civilizational Renewal"
    gloss:
        ← semantic compression required
end

PASS TheGreatFilterWindow:
    sutra: "← compress The Great Filter Window"
    gloss:
        ← semantic compression required
end

PASS IiiTechnologicalIndustrialPhases:
    sutra: "← compress III. TECHNOLOGICAL & INDUSTRIAL PHASES"
    gloss:
        ← semantic compression required
end

PASS PostindustrialAndHightechSociety:
    sutra: "← compress Post-Industrial and High-Tech Society"
    gloss:
        ← semantic compression required
end

PASS TheInformationAgeDigital:
    sutra: "← compress The Information Age / Digital Era / AI Era"
    gloss:
        ← semantic compression required
end

PASS TheSecondAtomicAge:
    sutra: "← compress The Second Atomic Age"
    gloss:
        ← semantic compression required
end

PASS TheSyntheticAgeBiological:
    sutra: "← compress The Synthetic Age (Biological Phase)"
    gloss:
        ← semantic compression required
end

PASS TheTechnocene:
    sutra: "← compress The Technocene"
    gloss:
        ← semantic compression required
end

PASS TheCyberneticAge:
    sutra: "← compress The Cybernetic Age"
    gloss:
        ← semantic compression required
end

PASS ComputeOverhangTheStack:
    sutra: "← compress Compute Overhang / The Stack"
    gloss:
        ← semantic compression required
end

PASS IvEconomicFinancialPhases:
    sutra: "← compress IV. ECONOMIC & FINANCIAL PHASES"
    gloss:
        ← semantic compression required
end

PASS TheGreatDecoupling:
    sutra: "← compress The Great Decoupling"
    gloss:
        ← semantic compression required
end

PASS TheEndOfThe:
    sutra: "← compress The End of the Long-Term Debt Cycle"
    gloss:
        ← semantic compression required
end

PASS PostscarcityEconomyAndEconomic:
    sutra: "← compress Post-Scarcity Economy and Economic Singularity"
    gloss:
        ← semantic compression required
end

PASS SlowbalizationFragmentation:
    sutra: "← compress Slowbalization / Fragmentation"
    gloss:
        ← semantic compression required
end

PASS TheGreatTransitionSociotechnicaleconomic:
    sutra: "← compress The Great Transition (Socio-Technical-Economic)"
    gloss:
        ← semantic compression required
end

PASS VGeopoliticalPhases:
    sutra: "← compress V. GEOPOLITICAL PHASES"
    gloss:
        ← semantic compression required
end

PASS MultipolarGlobalSystemGeopolitical:
    sutra: "← compress Multipolar Global System / Geopolitical Transition"
    gloss:
        ← semantic compression required
end

PASS ViInformationEpistemicPhases:
    sutra: "← compress VI. INFORMATION & EPISTEMIC PHASES"
    gloss:
        ← semantic compression required
end

PASS TheEpistemicCrisisPosttruth:
    sutra: "← compress The Epistemic Crisis / Post-Truth Era"
    gloss:
        ← semantic compression required
end

PASS InformationDisorderInfodemicEra:
    sutra: "← compress Information Disorder / Infodemic Era"
    gloss:
        ← semantic compression required
end

PASS TheAttentionEconomy:
    sutra: "← compress The Attention Economy"
    gloss:
        ← semantic compression required
end

PASS ViiDemographicSpatialPhases:
    sutra: "← compress VII. DEMOGRAPHIC & SPATIAL PHASES"
    gloss:
        ← semantic compression required
end

PASS TheGlobalDemographicTransition:
    sutra: "← compress The Global Demographic Transition / Demographic Crossroads"
    gloss:
        ← semantic compression required
end

PASS ThePlanetaryUrbanPhase:
    sutra: "← compress The Planetary Urban Phase / Urban Century"
    gloss:
        ← semantic compression required
end

PASS ViiiCulturalPsychologicalPhases:
    sutra: "← compress VIII. CULTURAL & PSYCHOLOGICAL PHASES"
    gloss:
        ← semantic compression required
end

PASS TheInterregnum:
    sutra: "← compress The Interregnum"
    gloss:
        ← semantic compression required
end

PASS MetamodernismCulturalPhase:
    sutra: "← compress Metamodernism (Cultural Phase)"
    gloss:
        ← semantic compression required
end

PASS TheAgeOfExhaustion:
    sutra: "← compress The Age of Exhaustion / Cultural Belatedness"
    gloss:
        ← semantic compression required
end

PASS TheMeaningCrisis:
    sutra: "← compress The Meaning Crisis"
    gloss:
        ← semantic compression required
end

PASS ThePhaseOfReembodiment:
    sutra: "← compress The Phase of Re-Embodiment"
    gloss:
        ← semantic compression required
end

PASS TheReenchantmentMythopoeticPhase:
    sutra: "← compress The Re-Enchantment (Mythopoetic Phase)"
    gloss:
        ← semantic compression required
end

PASS IxEvolutionaryConsciousnessPhases:
    sutra: "← compress IX. EVOLUTIONARY & CONSCIOUSNESS PHASES"
    gloss:
        ← semantic compression required
end

PASS AxialAge20Second:
    sutra: "← compress Axial Age 2.0 / Second Axial Age"
    gloss:
        ← semantic compression required
end

PASS TheTransitionToThe:
    sutra: "← compress The Transition to the Noosphere"
    gloss:
        ← semantic compression required
end

PASS ThePosthumanTransition:
    sutra: "← compress The Posthuman Transition"
    gloss:
        ← semantic compression required
end

PASS TheTransitionalSingularity:
    sutra: "← compress The Transitional Singularity"
    gloss:
        ← semantic compression required
end

PASS PostsingularityAndSymbioticFuture:
    sutra: "← compress Post-Singularity and Symbiotic Future"
    gloss:
        ← semantic compression required
end

PASS XInstitutionalGovernancePhases:
    sutra: "← compress X. INSTITUTIONAL & GOVERNANCE PHASES"
    gloss:
        ← semantic compression required
end

PASS TheGreatHollowingVetocracy:
    sutra: "← compress The Great Hollowing / Vetocracy"
    gloss:
        ← semantic compression required
end

PASS ThePostnormalEra:
    sutra: "← compress The Postnormal Era"
    gloss:
        ← semantic compression required
end

PASS XiCyclicPeriodizationFrameworks:
    sutra: "← compress XI. CYCLIC & PERIODIZATION FRAMEWORKS"
    gloss:
        ← semantic compression required
end

PASS TheFifthKondratieffWave:
    sutra: "← compress The Fifth Kondratieff Wave"
    gloss:
        ← semantic compression required
end

PASS PeakModernityEndOf:
    sutra: "← compress Peak Modernity / End of Modernity"
    gloss:
        ← semantic compression required
end

PASS CivilizationalTransformation20202050:
    sutra: "← compress Civilizational Transformation (2020-2050)"
    gloss:
        ← semantic compression required
end

PASS ContemporaryPeriodizationModels:
    sutra: "← compress Contemporary Periodization Models"
    gloss:
        ← semantic compression required
end

PASS XiiParadoxesTensions:
    sutra: "← compress XII. PARADOXES & TENSIONS"
    gloss:
        ← semantic compression required
end

PASS TheGreatStagnationVs:
    sutra: "← compress The Great Stagnation vs. Intelligence Explosion Paradox"
    gloss:
        ← semantic compression required
end

PASS TheBiosecurityDilemma:
    sutra: "← compress The Biosecurity Dilemma"
    gloss:
        ← semantic compression required
end

PASS XiiiEnergyEcologyThermodynamics:
    sutra: "← compress XIII. ENERGY, ECOLOGY & THERMODYNAMICS"
    gloss:
        ← semantic compression required
end

PASS TheNetzeroTransition:
    sutra: "← compress The Net-Zero Transition"
    gloss:
        ← semantic compression required
end

PASS TheEntropicThresholdThermodynamiccivilizational:
    sutra: "← compress The Entropic Threshold (Thermodynamic-Civilizational Phase)"
    gloss:
        ← semantic compression required
end

PASS XivSpecificDomainTransitions:
    sutra: "← compress XIV. SPECIFIC DOMAIN TRANSITIONS"
    gloss:
        ← semantic compression required
end

PASS TheNewSpaceEconomy:
    sutra: "← compress The New Space Economy"
    gloss:
        ← semantic compression required
end

PASS TheNetworkSociety:
    sutra: "← compress The Network Society"
    gloss:
        ← semantic compression required
end

PASS TheCosmotechnicTurn:
    sutra: "← compress The Cosmotechnic Turn"
    gloss:
        ← semantic compression required
end

PASS TheChronicDiseasePandemic:
    sutra: "← compress The Chronic Disease Pandemic (Biological-Medical)"
    gloss:
        ← semantic compression required
end

PASS XvEmergentPrioritiesOrientations:
    sutra: "← compress XV. EMERGENT PRIORITIES & ORIENTATIONS"
    gloss:
        ← semantic compression required
end

PASS EmergingCivilizationalPriorities:
    sutra: "← compress Emerging Civilizational Priorities"
    gloss:
        ← semantic compression required
end

PASS TheGreatReintegrationPsychologicalcivilizational:
    sutra: "← compress The Great Re-Integration (Psychological-Civilizational)"
    gloss:
        ← semantic compression required
end

PASS SynthesisConvergentInsightsAcross:
    sutra: "← compress SYNTHESIS: CONVERGENT INSIGHTS ACROSS FRAMEWORKS"
    gloss:
        ← semantic compression required
end

PASS MetaobservationTheProliferationOf:
    sutra: "← compress META-OBSERVATION: THE PROLIFERATION OF FRAMEWORKS ITSELF"
    gloss:
        ← semantic compression required
end

PROC ApplicationProtocol:
    sutra: "← compress APPLICATION PROTOCOL"
    gloss:
        ← semantic compression required
end

PASS ForSourceTriageSources:
    sutra: "← compress For Source Triage (SOURCES/)"
    gloss:
        ← semantic compression required
end

PROC ForProcessingSourcesprocessed:
    sutra: "← compress For Processing (SOURCES/processed/)"
    gloss:
        ← semantic compression required
end

PASS ForCanonIntegration:
    sutra: "← compress For CANON Integration"
    gloss:
        ← semantic compression required
end

PASS ClosingReflection:
    sutra: "← compress CLOSING REFLECTION"
    gloss:
        ← semantic compression required
end

PASS VersionHistory:
    sutra: "← compress VERSION HISTORY"
    gloss:
        ← semantic compression required
end

PASS IntegratedSources:
    sutra: "← compress INTEGRATED SOURCES"
    gloss:
        ← semantic compression required
end

PASS Source20250320041BenjaminBrattonA:
    sutra: "← compress SOURCE-20250320-041: Benjamin Bratton - "A Philosophy of Planetary Computation""
    gloss:
        ← semantic compression required
end

PASS Source20250528043SaraImariWalker:
    sutra: "← compress SOURCE-20250528-043: Sara Imari Walker - "An Informational Theory of Life""
    gloss:
        ← semantic compression required
end

PASS Source20250403051ScottAlexanderDaniel:
    sutra: "← compress SOURCE-20250403-051: Scott Alexander & Daniel Kokotajlo - "AI 2027""
    gloss:
        ← semantic compression required
end

PASS Source20251031002ElonMuskAlgorithms:
    sutra: "← compress SOURCE-20251031-002: Elon Musk - "Algorithms, Truth, and Robotaxi Economics""
    gloss:
        ← semantic compression required
end
