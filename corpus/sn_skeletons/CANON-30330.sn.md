---
id: [[CANON-30330]]
name: Research Protocols
tier: lattice
chain: intelligence
celestial_type: asteroid
volatility_band: dynamic
sn_version: 2.0
parent: CANON-30300
requires: ['CANON-30000', 'CANON-30300']
entities: ['Source Triad Method (PROTO)', 'Verdicting Process (PROTO)', 'Decision-Bearing Questions (CON)', 'Frontier Tracking (WF)', 'Capability Contract Audit (PROTO)', 'Research Anti-Patterns (CON)', 'Confidence Levels (MET)']
original_words: 1525
original_lines: 362
---

# Κ-30330: Research Protocols (SN)

PROC Canon30330ResearchProtocols:
    sutra: "← compress CANON-30330: RESEARCH PROTOCOLS"
    gloss:
        ← semantic compression required
end

NORM MethodologiesAndQualityStandards:
    sutra: "← compress Methodologies and Quality Standards"
    gloss:
        ← semantic compression required
end

PASS ICoreMethodology:
    sutra: "← compress I. Core Methodology"
    gloss:
        ← semantic compression required
end

PASS TheSourceTriadMethod:
    sutra: "← compress The Source Triad Method"
    gloss:
        ← semantic compression required
end

PASS CelestialNavigation:
    sutra: "← compress CELESTIAL NAVIGATION"
    gloss:
        ← semantic compression required
end

PASS DecisionbearingQuestionsFirst:
    sutra: "← compress Decision-Bearing Questions First"
    gloss:
        ← semantic compression required
end

PROC TheVerdictingProcess:
    sutra: "← compress The Verdicting Process"
    gloss:
        ← semantic compression required
end

PASS LedgeringAndContradictionManagement:
    sutra: "← compress Ledgering and Contradiction Management"
    gloss:
        ← semantic compression required
end

PASS IiResearchAntipatterns:
    sutra: "← compress II. Research Anti-Patterns"
    gloss:
        ← semantic compression required
end

PASS BenchmarkShopping:
    sutra: "← compress Benchmark Shopping"
    gloss:
        ← semantic compression required
end

PASS LinkDumping:
    sutra: "← compress Link Dumping"
    gloss:
        ← semantic compression required
end

PASS HypeAmplification:
    sutra: "← compress Hype Amplification"
    gloss:
        ← semantic compression required
end

PASS RecencyBias:
    sutra: "← compress Recency Bias"
    gloss:
        ← semantic compression required
end

PASS ConfirmationBias:
    sutra: "← compress Confirmation Bias"
    gloss:
        ← semantic compression required
end

PASS IiiFrontierTracking:
    sutra: "← compress III. Frontier Tracking"
    gloss:
        ← semantic compression required
end

PASS TargetDomains:
    sutra: "← compress Target Domains"
    gloss:
        ← semantic compression required
end

PASS TrackingCadence:
    sutra: "← compress Tracking Cadence"
    gloss:
        ← semantic compression required
end

PASS FrontierMapStructure:
    sutra: "← compress Frontier Map Structure"
    gloss:
        ← semantic compression required
end

NORM IvQualityStandards:
    sutra: "← compress IV. Quality Standards"
    gloss:
        ← semantic compression required
end

PASS ResearchQualityChecklist:
    sutra: "← compress Research Quality Checklist"
    gloss:
        ← semantic compression required
end

NORM ReportStandards:
    sutra: "← compress Report Standards"
    gloss:
        ← semantic compression required
end

PROC PeerReviewProtocol:
    sutra: "← compress Peer Review Protocol"
    gloss:
        ← semantic compression required
end

PASS VContextspecificPatterns:
    sutra: "← compress V. Context-Specific Patterns"
    gloss:
        ← semantic compression required
end

PASS AiCapabilityEvaluation:
    sutra: "← compress AI Capability Evaluation"
    gloss:
        ← semantic compression required
end

PASS ParadigmShiftAssessment:
    sutra: "← compress Paradigm Shift Assessment"
    gloss:
        ← semantic compression required
end

PASS InfrastructureServiceEvaluation:
    sutra: "← compress Infrastructure Service Evaluation"
    gloss:
        ← semantic compression required
end

PASS ToolPrimitiveExtraction:
    sutra: "← compress Tool Primitive Extraction"
    gloss:
        ← semantic compression required
end

PASS ViOutputFormats:
    sutra: "← compress VI. Output Formats"
    gloss:
        ← semantic compression required
end

PASS ResearchReportComprehensive:
    sutra: "← compress Research Report (Comprehensive)"
    gloss:
        ← semantic compression required
end

PASS ResearchLogEntryQuick:
    sutra: "← compress Research Log Entry (Quick)"
    gloss:
        ← semantic compression required
end

PASS DecisionBriefActionoriented:
    sutra: "← compress Decision Brief (Action-Oriented)"
    gloss:
        ← semantic compression required
end

PASS ViiCapabilityContractAudit:
    sutra: "← compress VII. Capability Contract Audit"
    gloss:
        ← semantic compression required
end

PASS AuditCadence:
    sutra: "← compress Audit Cadence"
    gloss:
        ← semantic compression required
end

PASS AuditChecklist:
    sutra: "← compress Audit Checklist"
    gloss:
        ← semantic compression required
end

PASS FindingsClassification:
    sutra: "← compress Findings Classification"
    gloss:
        ← semantic compression required
end

PASS ResponseActions:
    sutra: "← compress Response Actions"
    gloss:
        ← semantic compression required
end

PASS ViiiOperationalResearchExcellence:
    sutra: "← compress VIII. Operational Research Excellence"
    gloss:
        ← semantic compression required
end

PASS VersionHistory:
    sutra: "← compress VERSION HISTORY"
    gloss:
        ← semantic compression required
end

PASS Crossreferences:
    sutra: "← compress CROSS-REFERENCES"
    gloss:
        ← semantic compression required
end
