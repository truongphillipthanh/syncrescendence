# Intention Compass
Type: file
First seen: Phase 1
Status: active

## What it is
The archaeological record of all intentions, patterns, and decisions in the constellation. Located at `orchestration/state/ARCH-INTENTION_COMPASS.md` (v3.5.0). Must be consulted before executing directives (Session Protocol).

## Relationships
- referenced_by: 30 edges in knowledge graph
- consulted_by: All agents at session start (Directive Initialization Protocol step 3)
- contains: INT-* intentions, INT-P* patterns
- recent_additions: INT-2413 (action nodes), INT-2414 (process mining), INT-2415 (agent self-memory), INT-2416 (scored context + TTL)
- location: orchestration/state/ARCH-INTENTION_COMPASS.md

## Current state
Active. v3.5.0. Heavily referenced (30 incoming edges). Contains the new intentions from Council 25 corpus-intention synthesis. Key patterns include INT-P034 (feedback loops > memory), INT-P035 (security = phase 0), INT-P036 (feedcrafting = RAS).
