# Legacy Sources Protocol Seeds

These are preserved protocol specimens copied from the archived `04-SOURCES` lane.

Use them as:

- feedstock-ingestion pedigree
- source qualification reference
- frontmatter and manifest seeds

Do not treat them as constitutional law by default. Compact upward deliberately into modern source-ingestion doctrine when the pattern is still valid.
