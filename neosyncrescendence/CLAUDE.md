# Claude Code Sandbox Surface

This directory is the live staging shell for Syncrescendence redesign.

Operate with these rules:

1. Stay inside `neosyncrescendence/` unless a task explicitly requires reading or copying pedigree from the legacy repo.
2. Treat [AGENTS.md](/Users/system/syncrescendence/neosyncrescendence/AGENTS.md) as the constitutional source.
3. Prefer lawful lanes over ad hoc placement:
   - `communications/`
   - `executive/`
   - `program/`
   - `offices/`
   - `playbooks/`
   - `operators/`
   - `runtime/`
   - `pedigree/`
   - `validated-patterns/`
4. Compact repeated patterns into playbooks and operators.
5. Keep the root clean.
6. Use the sandbox to stage the new shell; do not casually mutate the legacy repo as if it were the active redesign surface.
