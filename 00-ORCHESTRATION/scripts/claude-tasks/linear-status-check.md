You are running as a scheduled background task for the Syncrescendence system.

## Task: Linear SYN Team Status Check

Fetch the current status of all active Linear issues in the SYN team and produce a status report.

### Steps

1. Fetch all issues NOT in "Done" status from the Linear SYN team using their GraphQL API:

```bash
curl -s -X POST https://api.linear.app/graphql \
  -H "Content-Type: application/json" \
  -H "Authorization: $LINEAR_API_KEY" \
  -d '{"query":"{ team(id:\"7b039eee-f9c3-4602-8813-0e1520eba386\") { issues(filter:{state:{name:{nin:[\"Done\",\"Canceled\"]}}}) { nodes { identifier title state { name } priority assignee { name } labels { nodes { name } } updatedAt } } } }"}'
```

Note: The LINEAR_API_KEY environment variable is already set. Use it directly as the Bearer token value.

2. Parse the JSON response using jq.

3. Write a summary report to `/Users/home/Desktop/syncrescendence/-INBOX/commander/00-INBOX0/TASK-LINEAR-STATUS-$(date +%Y%m%d%H%M).md` with this format:

```markdown
# Linear SYN Status Report — YYYY-MM-DD HH:MM

**Generated by**: claudecron/linear-status-check
**Status**: PENDING

## Summary
- Total active issues: N
- In Progress: N
- Todo: N
- Backlog: N

## In Progress
- SYN-XX: Title (Priority, Assignee)

## Blockers / Stale
(Issues not updated in >7 days)

## Recommendations
(Brief suggestions for unblocking or prioritization)
```

4. Print the summary to stdout as well.

Keep the report concise. Focus on actionable information.
