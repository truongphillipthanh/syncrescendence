# Implementation Tranche AS — Acumen CC93 First Manifest And Strict Admission

- **Tranche**: AS
- **program_id**: `TRANCHE-AS`
- **intent_refs**: `INT-SHELL-001`, `INT-SHELL-003`, `INT-SHELL-005`
- **rosetta_refs**: `operator`, `runtime`, `verification`, `knowledge lane`, `feedstock`, `registry`, `repo sovereignty`
- **execution_surface**: `program/IMPLEMENTATION-TRANCHE-AS-ACUMEN-CC93-FIRST-MANIFEST-AND-STRICT-ADMISSION.md`
- **lineage_or_origin**: Campaign 13 synthesis and the transition from lawful inbound-feed architecture to first observed admission proof

## Purpose

Close the gap between landed inbound-feed law and actual widened intake evidence by producing the first lawful manifest witness, strict policy-complete seed rows, and first strict-clean admission path.

## Tasks

1. backfill one specimen manifest pair and feedstock witness lineage for the current Acumen/YouTube intake surface
2. teach the importer to emit all strict policy-binding fields required for admission
3. prove a first strict-clean admission or legacy-row backfill path into the live registry
4. widen telemetry so it can distinguish legacy, specimen-proven, and widened admitted state
5. add stronger shadow-intake guardrails around later-chain raw-intake drift
6. prepare the first browser-bound live capture wave once the specimen path is proven

## Promotion / Completion Criteria

- at least one manifest pair exists beyond README scaffolding
- the manifest has preserved raw lineage into feedstock witness lanes
- importer-emitted seed rows carry strict policy bindings
- at least one row can validate cleanly under strict policy admission or a bounded legacy-backfill path is proven
- telemetry can report admitted widened state truthfully rather than only legacy registry state
- browser-bound capture work is now operating over a proven repo-local admission path rather than over unproven doctrine
