# Runtime Webshell Artifacts

This lane stores transient runtime artifacts emitted by the development webshell.

Current capture lane:

- `callbacks/inbox/` for raw callback payload captures

Artifacts here are operational intake, not canonical doctrine.
